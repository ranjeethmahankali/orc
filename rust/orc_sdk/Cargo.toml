[package]
name = "orc_sdk"
version = "0.1.0"
edition = "2024"

[features]
derive = ["orc_sdk_derive"]

[dependencies]
orc_sdk_derive = { path = "derive_macros", optional = true }
libloading = "0.8"

# Using this to serialize the DAG workflows for now. Doesn't inherit a tree of dependencies. May
# rethink this later.
rmp = "0.8"