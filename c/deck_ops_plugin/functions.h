#include <orc_abi.h>

void flatten_deck(uint64_t         ctx,
                  OrcHandle const *inputs,
                  uint64_t         n_inputs,
                  OrcHandle       *outputs,
                  uint64_t         n_outputs);
