
Archived entries from file /home/rnjth94/dev/orc/PROJECT.org


* DONE Unit System
  CLOSED: [2026-07-28 Tue 09:13]
  :PROPERTIES:
  :CREATED:  2026-06-06 Sat 19:20
  :ARCHIVE_TIME: 2026-08-02 Sun 14:17
  :ARCHIVE_FILE: ~/dev/orc/PROJECT.org
  :ARCHIVE_CATEGORY: PROJECT
  :ARCHIVE_TODO: DONE
  :END:
  :LOGBOOK:
  - State "DONE"       from "TODO"       [2026-07-28 Tue 09:13]
  :END:

  I awkwardly shoved the unit system in the DeckHandle. This needs a lot of
  thought and needs to be done properly.

  <2026-07-28 Tue 09:12> I tweaked this as I went along. It is now in a decent shape. Unless some
  very specific requirements come up, the current state is good enough to replace what we have in
  Kratos.

* DONE tgah09 - Combinations: Helper functions to stride along decks
  CLOSED: [2026-06-08 Mon 18:26]
  :PROPERTIES:
  :CREATED:  2026-06-07 Sun 08:36
  :ARCHIVE_TIME: 2026-08-02 Sun 14:17
  :ARCHIVE_FILE: ~/dev/orc/PROJECT.org
  :ARCHIVE_CATEGORY: PROJECT
  :ARCHIVE_TODO: DONE
  :END:
  :LOGBOOK:
  - State "DONE"       from "NEXT"       [2026-06-08 Mon 18:26]
  - State "NEXT"       from "TODO"       [2026-06-07 Sun 16:11]
  :END:

  <2026-06-07 Sun 08:36> Write a stubbed out API for combinations. Starting to
  take shape in C.

  <2026-06-07 Sun 16:11> Made some more progress. Have a rough sketch of what
  the API should look like, and the mechanism.

  <2026-06-07 Sun 21:23> Finished writing out the entire logic of combinations
  with zero testing done so far. Made a lot of progress. This also forced me to
  get more clarity on the ownership of decks and handles.

  <2026-06-08 Mon 17:32> Tested with several combinations of inputs. Working
  beautifully.

  <2026-06-14 Sun 18:08> Also wrote and tested a Rust implementation, that
  resembles the C implementation.

** DONE Use matrices for storage of views and writers
   CLOSED: [2026-06-08 Mon 18:26]
   :PROPERTIES:
   :CREATED:  2026-06-07 Sun 20:11
   :END:
   :LOGBOOK:
   - State "DONE"       from "TODO"       [2026-06-08 Mon 18:26]
   :END:

   [[file:./c/src/orc_sdk.c::// TODO - Use matrices for storage of views and writers. view_stacks, and][Change here]]

   =view_stacks=, and =writer_stacks= arrays nested arrays are rectangular, so it
   should be allocated as one buffer with matrix style indexing. This has a lot
   of benefits:
   - This will move all the potential allocation failures all into one place
     inside comb_init, because that is the only place allocations
     happen.
   - Currently I have a set of asserts to make sure the stacks corresponding to
     different inputs and outputs are all of the same depth. All that mess, can
     be simplified by having just one integer to keep track of the depth of all
     stacks. This design will make a bunch of invalid states impossible to
     represent in this datastructure.

  <2026-06-08 Mon> Just did this refactor. Simplified the code, works
  beautifully. The test cases of list processing pass.

** DONE comb_init should take explicit array lengths
   CLOSED: [2026-06-08 Mon 14:07]
   :PROPERTIES:
   :CREATED:  2026-06-08 Mon 13:58
   :END:
   :LOGBOOK:
   - State "DONE"       from "TODO"       [2026-06-08 Mon 14:07]
   :END:

   [[file:c/src/orc_sdk.c::// TODO: Update this function to take input and output counts explicitly instead of][Change here]]

   <2026-06-08 Mon 14:07> This was trivial, just did it, and it worked.

* DONE thlu2u - Generic Container Operations
  CLOSED: [2026-07-03 Fri 19:12]
  :PROPERTIES:
  :CREATED:  2026-07-03 Fri 11:08
  :ARCHIVE_TIME: 2026-08-02 Sun 14:17
  :ARCHIVE_FILE: ~/dev/orc/PROJECT.org
  :ARCHIVE_CATEGORY: PROJECT
  :ARCHIVE_TODO: DONE
  :END:
  :LOGBOOK:
  - State "DONE"       from "NEXT"       [2026-07-03 Fri 19:12]
  - State "NEXT"       from "TODO"       [2026-07-03 Fri 11:13]
  :END:

  A plugin needs to be able to implement generic container operations that work
  with datatypes that are not owned by that plugin, across the DLL
  boundary. This is not so trivial. While trying to figure out the plugin
  architecture, I came up with the idea of using an API that is similar to
  LLVM's vector shuffle instruction.

  <2026-07-03 Fri 12:16> Wrote an untested implementation in C to create decks
  from proxy decks.

  <2026-07-03 Fri 19:12> Tested it with LLM generated tests, and fixed
  issues. Ready to merge and mark this done.

* DONE tgp7lf - Plugin Architecture
  CLOSED: [2026-07-27 Mon 08:45]
  :PROPERTIES:
  :CREATED:  2026-06-15 Mon 20:19
  :ARCHIVE_TIME: 2026-08-02 Sun 14:18
  :ARCHIVE_FILE: ~/dev/orc/PROJECT.org
  :ARCHIVE_CATEGORY: PROJECT
  :ARCHIVE_TODO: DONE
  :END:
  :LOGBOOK:
  - State "DONE"       from "NEXT"       [2026-07-27 Mon 08:45]
  - State "NEXT"       from "TODO"       [2026-07-04 Sat 11:42]
  - State "TODO"       from "NEXT"       [2026-07-03 Fri 11:08]
  - State "NEXT"       from "TODO"       [2026-06-15 Mon 20:30]
  :END:

  The basic datastructure stuff is coming out nicely, but still, all the C code is one place, and
  the all the Rust code is in one crate. I need to make clean boundary between the host programs,
  the ABI layer, and the plugin binaries. The Rust side of this task is non-trivial and will require
  some thought.

  <2026-07-03 Fri 11:10> Started figuring out the solution for generic containers, while working
  through this task. Will get that done as a separate task and return to this later.

  <2026-07-04 Sat 11:42> I want to use bindgen to generate the ABI layer in all languages from a
  single source of truth.

  <2026-07-23 Thu 23:42> Made decent progress using bindgen to establish a single source of truth
  for the ABI a few weeks ago. Also making slight progress toward a host / plugin split in the Rust
  SDK. The =orc_plugin= macro is the key to make this work. Every Rust plugin will need to call this
  macro exactly once in their crate. The macro expansion will implement ABI compatible C functions
  based on Rust trait implementation.

  The proxy deck stuff (how generic container operations work) is a little behind in Rust compared
  to the C SDK. Left some todos in the code. I need to finish that up before continuing with the
  API.

  <2026-07-25 Sat 16:08> Untested stubs mostly sketched out for dealing with proxy decks in
  Rust. Need to separate out the SDK library crate from concrete plugin crates to be able to test
  it.

  <2026-07-25 Sat 19:52> Started implementing =DeckRegistry=. Made me realize assumptions around
  concurrency require a lot more thought. Host programs will generally take responsibility for
  running things in the correct order, keeping track of dependencies etc. But the plugins also need
  to be able to assert their own safety, locks etc. I am trying to figure out if there is a common
  denominator of behavior + checking that all plugins will use, that I can include in the SDK.

  <2026-07-26 Sun 23:02> Figured out quite a few details. Made a lot of progress today. The registry
  is now super generic, and can be used with any type, not just decks. Got a toy plugin crate to
  build in Rust, and a toy host program to build in Rust. Verified that the crate can load the
  shared library, and run the plugin function properly. The host is also able to load the metadata
  for the plugin function. I think I am close to merging this. Will do a final review, and add some
  tests before merging.

  <2026-07-27 Mon 08:44> This is looking pretty good. Could use some more tests, but not important
  right now. I will let more things take shape before nailing down testing.

* CLOSED Rendering Geometry
  CLOSED: [2026-07-28 Tue 09:16]
  :PROPERTIES:
  :CREATED:  2026-06-06 Sat 19:21
  :ARCHIVE_TIME: 2026-08-03 Mon 18:05
  :ARCHIVE_FILE: ~/dev/orc/PROJECT.org
  :ARCHIVE_CATEGORY: PROJECT
  :ARCHIVE_TODO: CLOSED
  :END:
  :LOGBOOK:
  - State "CLOSED"     from "TODO"       [2026-07-28 Tue 09:16]
  :END:

  The current rendering API in [[file:c/src/orc_abi.h::rendering_api][the current ffi]] is just a stub. While supporting
  static rendering primitives is simple, supporting implicit rendering requires
  a lot more thought.

  <2026-07-28 Tue 09:14> I changed my mind about this. I realized each host application will have
  very specialized rendering needs, and those needs cannot be met by a single API. So we're better
  off going the Parasolid route. Just expose functions that return data in a form that the host can
  render. E.g. meshes should return triangles, and implicit bodies should return a handle to some
  type of evaluator that can be used fill up voxel grids. In theory, should plug in to a system like
  Aether just fine. I will close this for now.

* DONE tj2fch - OrcHandle type_id rework
  CLOSED: [2026-08-01 Sat 18:36]
  :PROPERTIES:
  :CREATED:  2026-07-31 Fri 20:43
  :ARCHIVE_TIME: 2026-08-03 Mon 18:05
  :ARCHIVE_FILE: ~/dev/orc/PROJECT.org
  :ARCHIVE_CATEGORY: PROJECT
  :ARCHIVE_TODO: DONE
  :END:
  :LOGBOOK:
  - State "DONE"       from "NEXT"       [2026-08-01 Sat 18:36]
  - State "NEXT"       from "TODO"       [2026-08-01 Sat 17:39]
  :END:

  Need something that allows plugins to receive the host side type id, and allows plugins to receive
  handles containing data from other plugins.

  <2026-08-01 Sat 18:35> Needs more testing as I build more plugins. In as good of a shape as it can
  be at this early stage. Preparing to merge.

** DONE ABI / DLL Versioning
   CLOSED: [2026-08-01 Sat 17:40]
   :PROPERTIES:
   :CREATED:  2026-07-31 Fri 18:58
   :END:
   :LOGBOOK:
   - State "DONE"       from "NEXT"       [2026-08-01 Sat 17:40]
   - State "NEXT"       from "TODO"       [2026-07-31 Fri 19:29]
   :END:

   ABI itself should be protected from changes as much as possible. When it changes, all binaries,
   plugins, and DLLs must be recompiled. Some minor changes like adding a new function might not
   require this, but we need a water tight strategy to avoid loading running binaries that target a
   different ABI than the host. That can cause crashes.

   My first idea is to include a version integer in the header. That integer can pack all three
   values of a semantic version number in the bits of a 64 bit integer.

   <2026-07-31 Fri 19:29> Trying to take a crack at this whole prototyping demo apps.

   <2026-08-01 Sat 17:39> Turning this into a subtask, because it will get merged as part of another
   task. Marking it done. The design I have on this branch seems good enough for now.


* DONE tj2fe7 - Proxy decks through the ABI
  CLOSED: [2026-08-02 Sun 11:23]
  :PROPERTIES:
  :CREATED:  2026-07-31 Fri 20:44
  :ARCHIVE_TIME: 2026-08-05 Wed 19:05
  :ARCHIVE_FILE: d:/dev/orc/PROJECT.org
  :ARCHIVE_CATEGORY: PROJECT
  :ARCHIVE_TODO: DONE
  :END:
  :LOGBOOK:
  - State "DONE"       from "NEXT"       [2026-08-02 Sun 11:23]
  - State "NEXT"       from "TODO"       [2026-08-01 Sat 18:50]
  :END:

  Currently proxy deck stuff is not fully fleshed out through the ABI. Requires a bit more thought
  and [[*tj2fch - OrcHandle type_id rework][type_id rework]] needs to happen before this. I will just flesh this out first in the Rust SDK,
  and Rust examples. The C SDK has not taken shape yet.

  <2026-08-01 Sat 21:08> Started implementing it. Made some progress. Close to finishing. But hit a
  small snag. I realized that the refactor I just made to the type id system changes some of the
  ownership semantics. It allows many plugins to deal with the same type ids. Which means the host
  cannot clearly determine which plugin is supposed to free a deck. The deck MUST be freed by the
  same plugin that created it. Because only that plugin knows the implementation of the deck
  datastructure it used. Which means each handle needs to contain a pointer to a free function, that
  takes the u64 handle, and returns an error. The dll need not export the free function. This
  changes the ABI design. I need to figure this out before making progress on the proxy stuff

  <2026-08-02 Sun 10:56> Updated the ownership semantics of handles. The handles now carry their own
  free functions. Finished implementing the proxy stuff in the Rust plugin. Not tested it yet. I am
  considering merging this in, and testing it later as I write more plugins.


* CLOSED More general use of handles for data that is not decks
  CLOSED: [2026-08-03 Mon 17:46]
  :PROPERTIES:
  :CREATED:  2026-08-02 Sun 20:05
  :ARCHIVE_TIME: 2026-08-05 Wed 19:05
  :ARCHIVE_FILE: d:/dev/orc/PROJECT.org
  :ARCHIVE_CATEGORY: PROJECT
  :ARCHIVE_TODO: CLOSED
  :END:
  :LOGBOOK:
  - State "CLOSED"     from "TODO"       [2026-08-03 Mon 17:46]
  :END:

  Plugins should be allowed to use the handles to point to data that is not a deck. All the deck
  specific pointers and parameters should not be read by the host or any other plugin when this is
  the case. I am thinking this can be achieved with either a boolean flag.

  This is making things more complicated than it is worth. I am going to put this off for now. I
  think the plugins that want to do this can still do it. They just need to populate the handle data
  to imply the deck has just 1 item. That is very easy to do. So adding a whole other property for
  that, and all the branching that comes with it, is not worth it.


* DONE tj5fbs - Figure out C SDK and write a simple plugin
  CLOSED: [2026-08-04 Tue 10:55]
  :PROPERTIES:
  :CREATED:  2026-08-02 Sun 11:09
  :ARCHIVE_TIME: 2026-08-05 Wed 19:05
  :ARCHIVE_FILE: d:/dev/orc/PROJECT.org
  :ARCHIVE_CATEGORY: PROJECT
  :ARCHIVE_TODO: DONE
  :END:
  :LOGBOOK:
  - State "DONE"       from "NEXT"       [2026-08-04 Tue 10:55]
  - State "NEXT"       from "TODO"       [2026-08-02 Sun 11:44]
  :END:

  The code for this is mostly in place. I just need to figure out the project structure, the
  experience of various plugins that want to build off of ~orc_sdk~, and that of plugins that don't
  want to build off of ~orc_sdk~ etc. I also need to figure out how to setup all these projects with
  the ~nob~ build system.

  Container operations plugin is a prime candidate for writing in C, because it doesn't need any
  special libraries. Don't want to add too many blocks at once, but just a few to make sure the
  proxy stuff is working nicely across the ABI.

  <2026-08-02 Sun 11:43> orc_sdk should not implement definitions anything from orc_abi.h. orc_abi
  should just contain pure forward declarations. It is up to each plugin how to implement
  those. orc_sdk should provide namespaced equivalents of the things in orc_abi, and each plugin can
  choose to simply call the orc_sdk functions if they don't want to write their own.

  - [X] namespace everything in orc_sdk.h with a prefix.

  <2026-08-02 Sun 14:02> LLM namespaced the entire public API of orc_sdk.

  <2026-08-02 Sun 22:38> Made more progress. Overhauled the project structure to build multiple C
  projects with nob. Created a stubbed out plugin project. Hashed out many details in regards to the
  SDK / plugin boundary. Addressed almost all TODOs that were in the SDK code.

  - [X] Upon plugin init, the plugin should set an allocator within the SDK, it it receives one from
    the host. All the SDK code should be scoured for alloc, free, and realloc calls. Those should be
    wired through the host's API. SDK should have the usual allcoators, if the host doesn't provide
    one.

  <2026-08-03 Mon 15:45> Did the allocator refactor. Tried to load the C plugin into a Rust
  host. Works on Linux, but didn't work on Windows. Turned out I was using cygwin-gcc with it's own
  runtime, and the dlls built that way don't work like that on Windows. So I ditched nob.h, and
  started using cmake. Now everything builds and runs on Windows and Linux. The DLL loads
  properly. Fixed many small gaps in the C SDK along the way. All that is left to do is to write a
  simple block in C. And test running DLLs with a custom allocator.

  - [X] Test running plugins with a custom allocator.
  - [X] Write simple container operation block in C.

  <2026-08-03 Mon 21:54> Made a lot of prgress. Rust OrcHandles now have RAII. Successfully tested
  a C plugin function for the first time. Also tested the ability to share data across the FFI
  boundary between C and Rust.


* DONE tj92sq - OrcHandle Lifetimes and Identity
  CLOSED: [2026-08-05 Wed 18:12]
  :PROPERTIES:
  :CREATED:  2026-08-04 Tue 09:32
  :ARCHIVE_TIME: 2026-08-05 Wed 19:05
  :ARCHIVE_FILE: d:/dev/orc/PROJECT.org
  :ARCHIVE_CATEGORY: PROJECT
  :ARCHIVE_TODO: DONE
  :END:
  :LOGBOOK:
  - State "DONE"       from "NEXT"       [2026-08-05 Wed 18:12]
  - State "NEXT"       from "TODO"       [2026-08-04 Tue 10:56]
  :END:

  Handles pointing to data from one plugin can be passed in as inputs to any other plugin. This
  works fine, because input handles are immutable. It is unclear what should happen when the host
  passes in one of those handles as an output handle. In the current state, we will get some type of
  UB / crash. This is a serious liability for the design and needs to be fixed ASAP.

  Whatever solution I come up with, must make it hard to do the wrong thing, without creating too
  much friction. Ideally the host keeps the output handles of each block separate and doesn't mix
  and match them, but that becomes less clear when dealing with container operation blocks, that
  request the host to allocate decks in other plugins, of types that it doesn't recognize.

  - Should the host think of the resulting deck as belonging to the plugin that implemented the
    container op, or the plugin that created the deck from a proxy deck?
  - What happens when the output datatype of a container operation is a primitive type that is
    recognized by all plugins and the host? Who is responsible for the output then?
  - The meaning of the ~handle~ property of ~OrcHandle~ can be different in each plugin. What if I
    take an OrcHandle from plugin A, and pass it to plugin B, which also just happens to contain
    something in it's registry with the same ~handle~?

  This all feels like a mess, and needs to be solved immediately, otherwise the whole C API effort
  can fall apart.

  Should the host take ownership of the ~handle~ property and assign it, and make sure it is
  globally unique? And the plugins just use the host assigned ~handle~ as a unique identifier?

  <2026-08-05 Wed 17:25> Did the refactor. At least I think I got everything. The LLM generated plan
  below was 20% useful. Did most of it by hand, because the LLM plan was riddled with bad
  choices. Now it is time to test the shit out of this new design, guarantee all the invariants, and
  move on.

  <2026-08-05 Wed 17:46> Will do some reviewing and thinking, but any bugs and issues in the current
  design will surface once I start testing.


* DONE tjctcr - Fix the generated variable names in Rust macros
  CLOSED: [2026-08-06 Thu 11:31]
  :PROPERTIES:
  :CREATED:  2026-08-06 Thu 11:20
  :ARCHIVE_TIME: 2026-08-06 Thu 17:39
  :ARCHIVE_FILE: d:/dev/orc/PROJECT.org
  :ARCHIVE_CATEGORY: PROJECT
  :ARCHIVE_TODO: DONE
  :END:
  :LOGBOOK:
  - State "DONE"       from "NEXT"       [2026-08-06 Thu 11:31]
  - State "NEXT"       from "TODO"       [2026-08-06 Thu 11:22]
  :END:

  The generated variable names are currently prefixed with __. That can throw off the linter,
  because it could mean unused variable. Instead the should be suffixed with a _.

* DONE tjbhvp - Testing and CI
  CLOSED: [2026-08-05 Wed 20:49]
  :PROPERTIES:
  :CREATED:  2026-08-03 Mon 17:47
  :ARCHIVE_TIME: 2026-08-06 Thu 17:47
  :ARCHIVE_FILE: d:/dev/orc/PROJECT.org
  :ARCHIVE_CATEGORY: PROJECT
  :ARCHIVE_TODO: DONE
  :END:
  :LOGBOOK:
  - State "DONE"       from "NEXT"       [2026-08-05 Wed 20:49]
  - State "NEXT"       from "DONE"       [2026-08-05 Wed 20:04]
  - State "DONE"       from "NEXT"       [2026-08-05 Wed 19:44]
  - State "NEXT"       from "TODO"       [2026-08-05 Wed 18:17]
  :END:

  I am making fast progress, adding lots of code. I have some tests, but I need to test a lot
  more. All the plugin code should be thoroughly tested. All the new SDK code should be tested. I
  should look through the ABI carefully, and make sure each feature is tested.

  <2026-08-05 Wed 18:28> Adding CI automation for the tests I have so far. The last merge broke all
  the Rust tests, but I didn't run them before merging.

** DONE Test plan
   CLOSED: [2026-08-05 Wed 19:44]
   :LOGBOOK:
   - State "DONE"       from              [2026-08-05 Wed 19:44]
   :END:

*** DONE ~rust/orc_sdk/src/util.rs~ — ~mod test~
    CLOSED: [2026-08-05 Wed 19:44]
    :LOGBOOK:
    - State "DONE"       from "NEXT"       [2026-08-05 Wed 19:44]
    - State "NEXT"       from "TODO"       [2026-08-05 Wed 19:11]
    :END:

    Tests are from the perspective of a plugin using the SDK utilities.

**** DONE DeckRegistry Tests
     CLOSED: [2026-08-05 Wed 19:11]
     :LOGBOOK:
     - State "DONE"       from "TODO"       [2026-08-05 Wed 19:11]
     :END:

    ~DeckRegistry~ — ownership and lifecycle:
    - ~alloc_fresh~ — alloc on empty slot: ~free_fn~ set, handle fields populated, ID survives
    - ~alloc_reuse_same_type~ — second alloc with same type is a no-op: ~Ok~, handle fields
      unchanged. Should verify that the old free function has not been called.
    - ~alloc_type_change~ — second alloc with different type: old deck gone, new type reflected in
      handle. Should verify that the old deck has been freed.
    - ~alloc_eviction_foreign~ — handle has a mock foreign ~free_fn~; alloc evicts it (mock called),
      then populates handle.
    - ~free_success~ — alloc then free: ~free_fn = None~, items cleared, ~handle.handle~ preserved
    - ~free_unregistered~ — free on ID not in registry returns ~InvalidHandle~
    - ~free_does_not_clear_handle_id~ — after free, ~handle.handle~ still has its original value
    - ~with_mut_borrows_and_mutates~ — after alloc, ~with_mut~ successfully downcasts and modifies
      the deck
    - ~with_mut_missing_id~ — ~with_mut~ on an unregistered ID returns ~InvalidHandle~
    - ~concurrent_alloc_free~ — multiple threads alloc/free disjoint IDs simultaneously, no data
      corruption... how do you verify there is no corruption?

**** DONE handle tests
     CLOSED: [2026-08-05 Wed 19:14]
     :LOGBOOK:
     - State "DONE"       from "TODO"       [2026-08-05 Wed 19:14]
     :END:

    ~update_handle_from_deck~:
    - ~fields_populated~ — items, marks, strides, type_id set from deck; ~handle.handle~ and
      ~free_fn~ untouched
    - ~empty_deck_gives_null_items~ — empty deck: items pointer is null, counts are zero, type_id
      still set
    - ~preserves_handle_id~ — ~handle.handle~ unchanged before and after call
    - ~preserves_free_fn~ — ~free_fn~ unchanged before and after call

    ~reset_handle~:
    - ~clears_data_fields_not_id~ — items/marks/free_fn cleared to null/None; ~handle.handle~
      unchanged
    - ~idempotent~ — calling twice is safe, same result

    ~ptr_from_slice~ / ~slice_from_ptr~ / ~slice_from_ptr_mut~:
    - ~empty_slice_gives_null~ — ~ptr_from_slice(&[])~ returns null
    - ~nonempty_round_trips~ — pointer from non-empty slice round-trips through ~slice_from_ptr~
    - ~null_ptr_gives_empty_slice~ — ~slice_from_ptr(null, N)~ returns ~&[]~
    - ~zero_len_gives_empty_slice~ — ~slice_from_ptr(valid, 0)~ returns ~&[]~

*** DONE ~rust/orc_sdk/src/host.rs~ — ~mod test~
    CLOSED: [2026-08-05 Wed 19:41]
    :LOGBOOK:
    - State "DONE"       from "NEXT"       [2026-08-05 Wed 19:41]
    - State "NEXT"       from "TODO"       [2026-08-05 Wed 19:15]
    :END:

    Tests are from the perspective of a host application loading and using plugins. These are
    integration tests that require the built plugin DLLs to be present at test time.

    - ~alloc_deck_populates_handle~ — ~plugin.alloc_deck(type_id, &mut handle)~: ~free_fn~ set,
      items non-null, ~handle.handle~ unchanged
    - ~free_deck_resets_handle~ — alloc followed by free: ~free_fn = None~, items null, ID unchanged

*** DONE ~rust/math_plugin/src/basic.rs~ — ~mod test~
    CLOSED: [2026-08-05 Wed 19:44]
    :LOGBOOK:
    - State "DONE"       from "NEXT"       [2026-08-05 Wed 19:44]
    - State "NEXT"       from "TODO"       [2026-08-05 Wed 19:41]
    :END:

    Tests call the plugin functions directly through the generated FFI entry points.  Input handles
    are view handles (~free_fn = None~); output handles are owned by the plugin.

    ~add~:
    - ~add_f64_elementwise~ — ~[1,2,3] + [10,20,30] = [11,22,33]~
    - ~add_f32_elementwise~ — same for f32
    - ~add_integer_types~ — u32, i64: ~[5] + [3] = [8]~
    - ~add_broadcast_scalar_to_list~ — depth-2 list input broadcasts correctly
    - ~add_mismatched_types~ — i32 input + f64 input → function does not write output (type
      mismatch)
    - ~add_mismatched_dimensions~ — dim vectors differ → ~InvalidDimensions~, output not written
    - ~add_wrong_n_inputs~ — 1 input instead of 2 → error logged, output not written
    - ~add_output_reuse_same_type~ — call add twice on same output slot: second call reuses deck, no
      leak
    - ~add_output_type_change~ — call add then mul on same output slot: handle transitions type
      cleanly

    ~mul~: same matrix as add with multiplication values.

    ~sub~ / ~div~:
    - ~sub_f64~ / ~div_f64~ — basic f64 correctness
    - ~sub_unsupported_type~ — sub(u32, u32) → type mismatch (sub is floats-only)
    - ~div_by_zero~ — ~[1.0] / [0.0]~ → produces IEEE 754 inf, no panic
    - ~div_unsupported_type~ — div(i32, i32) → type mismatch

    Handle lifetime / ownership invariants (all functions):
    - ~output_free_fn_set_after_call~ — after any function call, ~out.free_fn~ is
      ~Some(orc_deck_free)~
    - ~output_handle_id_preserved~ — ~out.handle~ unchanged before and after call
    - ~output_owned_by_plugin_registry~ — after call, plugin registry contains the output handle ID
    - ~input_handles_unaffected~ — input ~free_fn~ and ~items~ are unchanged after the call

    Output deck reuse / reallocation / update invariants (tested via ~add~ only):
    - ~add_reuse_same_type_handle_updated~ — when the output slot already holds a deck of the
      same type, ~add~ reuses it and still updates ~out.items~, ~out.n_items~, ~out.type_id~ to
      reflect what was written in this call
    - ~add_type_change_handle_updated~ — when the output slot holds a deck of a different type,
      ~add~ frees it, allocates a new one of the correct type, and the handle reflects the new
      type and data
    - ~add_clears_previous_data~ — calling ~add~ twice on the same output slot produces only the
      results of the second call; the deck is cleared and rewritten, not appended to


* DONE tjbrtc - Implement Container Op Blocks in C
  CLOSED: [2026-08-06 Thu 16:40]
  :PROPERTIES:
  :CREATED:  2026-08-03 Mon 18:22
  :ARCHIVE_TIME: 2026-08-09 Sun 00:20
  :ARCHIVE_FILE: ~/dev/orc/PROJECT.org
  :ARCHIVE_CATEGORY: PROJECT
  :ARCHIVE_TODO: DONE
  :END:
  :LOGBOOK:
  - State "DONE"       from "NEXT"       [2026-08-06 Thu 16:40]
  - State "NEXT"       from "TODO"       [2026-08-05 Wed 21:51]
  :END:

  To test all the variations of this across plugin boundaries, I might need to add some custom
  datatypes in a Rust plugin.

  <2026-08-06 Thu 16:39> This is working for basic primitive types, that don't require deferring the
  deck construction to the host. I just have one container op function at this time, but I want to
  wait till the ABI is more stable before I add more functions.

* DONE tjd838 - Improve Function Metadata
  CLOSED: [2026-08-07 Fri 14:25]
  :PROPERTIES:
  :CREATED:  2026-08-02 Sun 19:14
  :ARCHIVE_TIME: 2026-08-09 Sun 00:20
  :ARCHIVE_FILE: ~/dev/orc/PROJECT.org
  :ARCHIVE_CATEGORY: PROJECT
  :ARCHIVE_TODO: DONE
  :END:
  :LOGBOOK:
  - State "DONE"       from "NEXT"       [2026-08-07 Fri 14:25]
  - State "NEXT"       from "TODO"       [2026-08-07 Fri 09:14]
  :END:

  We want the function metadata to also contain information about how many inputs and outputs the
  function has, and what types. All of this has to be optional, and functions may choose to be
  wildly variadic and type agnostic. The metatdata should allow the functions to express that
  somehow.

  <2026-08-07 Fri 14:24> Was easy enough to do. Not much I can test right now. Just need to make
  sure this works as expected as more things take shape and I am able to test more things.

* DONE tjewz9 - A LISP like syntax with multiple outputs?
  CLOSED: [2026-08-07 Fri 23:56]
  :PROPERTIES:
  :CREATED:  2026-08-04 Tue 08:37
  :ARCHIVE_TIME: 2026-08-09 Sun 00:20
  :ARCHIVE_FILE: ~/dev/orc/PROJECT.org
  :ARCHIVE_CATEGORY: PROJECT
  :ARCHIVE_TODO: DONE
  :END:
  :LOGBOOK:
  - State "DONE"       from "DONE"       [2026-08-07 Fri 23:56]
  - State "DONE"       from "NEXT"       [2026-08-07 Fri 23:56]
  - State "NEXT"       from "TODO"       [2026-08-07 Fri 14:34]
  :END:

  Something that allows for multiple named inputs and outputs. When a block has just one output,
  they can be nested as you normally do in LISP.

  <2026-08-07 Fri 14:33> Having this will make it easy to really test functions in various
  combinations, type system edge cases etc. So doing this task before making more progress with the
  SDK, functions, and types.

  <2026-08-07 Fri 22:46> In the long run this might end up as a plaint text format to save DAGs. For
  now, I just need something that lets me easily compose, run and test DAGs. So I went with a
  ~eiche~ style declarative macro in Rust.

  - [ ] Get rid of the vec allocations in the kbb_dag macro.

  <2026-08-07 Fri 23:27> Added tests for the macro, and got all the tests passing by using a global
  atomic to assign the handle ids.

  - [X] The macro should return an error instead of doing unwrap / expect internally.

  <2026-08-07 Fri 23:55> Getting ready to merge, trying to get the tests passing on Windows. CMake
  output path related issues cause the DLLs to be not found by the test binaries. But now that is
  fixed.

* DONE tjgdan - Python bindings
  CLOSED: [2026-08-09 Sun 00:22]
  :PROPERTIES:
  :CREATED:  2026-08-08 Sat 08:50
  :ARCHIVE_TIME: 2026-08-12 Wed 16:15
  :ARCHIVE_FILE: d:/dev/orc/PROJECT.org
  :ARCHIVE_CATEGORY: PROJECT
  :ARCHIVE_TODO: DONE
  :END:
  :LOGBOOK:
  - State "DONE"       from "NEXT"       [2026-08-09 Sun 00:22]
  - State "NEXT"       from "TODO"       [2026-08-08 Sat 09:25]
  :END:

  Ideally, this solution won't be tied to one specific version of Python, and only uses things that
  are built in to Python, and works with shared libraries that are built by third parties, as long
  as they conform to the ABI.

  - [X] Figure out the basics of calling a DLL function from Python.
  - [X] Load a plugin into Python, convert strings, and metadata.
  - [X] Call plugin functions from Python and display the results.

  <2026-08-08 Sat 22:40> Got this all to work. Also implemented conversion between decks and Python
  nested lists. Also added tests for all the loaded plugin functions.

  <2026-08-08 Sat 23:27> Found a memory leak when running the example. Debugging it now.

  - [X] All unit tests should assert that none of the memory is leaked.

  <2026-08-08 Sat 23:36> It was not a leak. That was just the space reserved inside Rust plugin's
  global registry. That will be reclaimed when the process dies.

  <2026-08-09 Sun 00:21> Works well. The helper module will grow as I use it and test it more. This
  is more than enough for a proof of concept. Ready to merge.

** CLOSED Alternative Plan to Generate Python Bindings with syn crate
   CLOSED: [2026-08-09 Sun 00:17]
   :PROPERTIES:
   :CREATED:  2026-08-08 Sat 15:31
   :END:
   :LOGBOOK:
   - State "CLOSED"     from              [2026-08-09 Sun 00:17]
   :END:

   1. Add syn to [build-dependencies] in rust/orc_sdk/Cargo.toml — for parsing bindgen's Rust output
   2. In build.rs, after bindgen generates bindings:
      - Get the output as a string via bindings.to_string()
      - Parse it with syn::parse_file()
      - Walk the AST and emit Python ctypes code for:
     - pub const NAME: u64 = 0x12 → NAME = 0x12
     - #[repr(C)] pub struct Foo { ... } → class Foo(ctypes.Structure): _fields_ = [...]
     - pub type Name = u64 → Name = ctypes.c_uint64
     - Option<unsafe extern "C" fn(...)> fields → ctypes.CFUNCTYPE(...)
      - Handle forward declarations (e.g. OrcHandle used in OrcDeckFreeFn before its struct
        definition) — emit class OrcHandle(ctypes.Structure): pass first, set _fields_ later
   3. Write output to ../../python/bindings.py
   4. Add python/bindings.py to .gitignore — it's generated, not checked in

* DONE tjj8pq - Numpy integration with zero overhead
  CLOSED: [2026-08-10 Mon 00:02]
  :PROPERTIES:
  :CREATED:  2026-08-09 Sun 00:47
  :ARCHIVE_TIME: 2026-08-12 Wed 16:15
  :ARCHIVE_FILE: d:/dev/orc/PROJECT.org
  :ARCHIVE_CATEGORY: PROJECT
  :ARCHIVE_TODO: DONE
  :END:
  :LOGBOOK:
  - State "DONE"       from "NEXT"       [2026-08-10 Mon 00:02]
  - State "NEXT"       from "TODO"       [2026-08-09 Sun 22:39]
  :END:

  Just a curiosity that came up when speaking to Jonathan. He was using some complicated library
  setup with a complicated CMake setup with a weird build target to get it working. I reckon I can
  do this in a much simpler way with no additional dependencies and no additional moving parts,
  build system etc.

  <2026-08-09 Sun 22:52> Numpy has a convenient API that let's you pass a C pointer and create a
  "view" numpy array, or matrix. This is similar to the ~Eigen::Map~ api. It was fairly easy to use
  that API, create a numpy array, and confirm that the numpy array uses the same pointer address as
  the OrcHandle.items. That makes this a "zero copy", solution.

  <2026-08-09 Sun 23:23> Handled the various garbage collection scenarios, and made sure this is
  memory safe. Added tests to cover all these scenarios.

  <2026-08-10 Mon 00:00> Made several improvements to the API, hid the internal details of handles,
  allocation, lifetimes etc. and made it look like clean Python code from the outside. Orc workflows
  written in Python should now look like natural Python code, closely resembling numpy.

  <2026-08-10 Mon 00:01> Making sure the CI works, and all the tests pass now, with numpy properly
  installed on the CI machines. Ready to merge after that.


* DONE tjkesx - Test with custom types
  CLOSED: [2026-08-10 Mon 22:51]
  :PROPERTIES:
  :CREATED:  2026-08-08 Sat 08:50
  :ARCHIVE_TIME: 2026-08-12 Wed 16:15
  :ARCHIVE_FILE: d:/dev/orc/PROJECT.org
  :ARCHIVE_CATEGORY: PROJECT
  :ARCHIVE_TODO: DONE
  :END:
  :LOGBOOK:
  - State "DONE"       from "NEXT"       [2026-08-10 Mon 22:51]
  - State "NEXT"       from "DONE"       [2026-08-10 Mon 20:04]
  - State "DONE"       from "NEXT"       [2026-08-10 Mon 19:32]
  - State "NEXT"       from "TODO"       [2026-08-10 Mon 13:48]
  :END:

  <2026-08-10 Mon 15:37> Wrote some complex number functions and tested them in both Rust and
  Python.

  <2026-08-10 Mon 19:31> Seems to be working well. Also tested it in Python. Just need to add a
  couple more tests, then ready to merge.

  <2026-08-10 Mon 20:04> Running into something trickly. Linux CI fails. Need more time.

** DONE Test proxy functions with custom types across plugins
   CLOSED: [2026-08-10 Mon 19:32]
   :PROPERTIES:
   :CREATED:  2026-08-08 Sat 08:50
   :END:
   :LOGBOOK:
   - State "DONE"       from "NEXT"       [2026-08-10 Mon 19:32]
   - State "NEXT"       from "TODO"       [2026-08-10 Mon 19:17]
   :END:

   <2026-08-10 Mon 19:17> Got this working for the first time. Works fine.


* DONE tjlw5f - Python Improvements
  CLOSED: [2026-08-11 Tue 12:26]
  :PROPERTIES:
  :CREATED:  2026-08-11 Tue 09:00
  :ARCHIVE_TIME: 2026-08-12 Wed 16:15
  :ARCHIVE_FILE: d:/dev/orc/PROJECT.org
  :ARCHIVE_CATEGORY: PROJECT
  :ARCHIVE_TODO: DONE
  :END:
  :LOGBOOK:
  - State "DONE"       from "NEXT"       [2026-08-11 Tue 12:26]
  - State "NEXT"       from "TODO"       [2026-08-11 Tue 11:41]
  :END:

  - [X] Figure out ~n_out~ from ~load_plugins~.
  - [X] Use normal delete for OrcHandle outside tests. - This was already happening. Nothing to do.
  - [X] ~__all__~: Figure this out.

  <2026-08-11 Tue> These tasks are done. The ~__all__~ thing was not what I thought it was. I will
  get back to that later. For now I have a way without it.

  <2026-08-11 Tue 12:26> Ready to merge.


* DONE tjmcc9 - orc_map_fn Macro in Rust SDK
  CLOSED: [2026-08-12 Wed 15:11]
  :PROPERTIES:
  :CREATED:  2026-08-02 Sun 11:00
  :ARCHIVE_TIME: 2026-08-14 Fri 12:37
  :ARCHIVE_FILE: ~/dev/orc/PROJECT.org
  :ARCHIVE_CATEGORY: PROJECT
  :ARCHIVE_TODO: DONE
  :END:
  :LOGBOOK:
  - State "DONE"       from "NEXT"       [2026-08-12 Wed 15:11]
  - State "NEXT"       from "TODO"       [2026-08-11 Tue 14:50]
  :END:

  Some functions will just have one input and one output, and they operate on a single item, and
  produce a single item. These functions don't need the overhead of ~Combinations~. They can just
  ~map~ the ~items~ of the deck, and just copy the ~marks~ of the deck. And the Rust compiler can
  optimize that code much better. It will be nice to have another proc_macro specialized for these
  types of functions. Something like ~orc_map_fn~.

  <2026-08-12 Wed 14:44> Got this working, and made a few other improvements to both macros. But
  realized this type of macro limits the LSP support when writing the body of the function. So I am
  switching to an attribute macro that works similar to what I have now.

* DONE tjo88p - Don't use build.rs
  CLOSED: [2026-08-12 Wed 16:14]
  :PROPERTIES:
  :CREATED:  2026-08-09 Sun 00:18
  :ARCHIVE_TIME: 2026-08-14 Fri 12:37
  :ARCHIVE_FILE: ~/dev/orc/PROJECT.org
  :ARCHIVE_CATEGORY: PROJECT
  :ARCHIVE_TODO: DONE
  :END:
  :LOGBOOK:
  - State "DONE"       from "NEXT"       [2026-08-12 Wed 16:14]
  - State "NEXT"       from "TODO"       [2026-08-12 Wed 15:17]
  :END:

  - Pull the bindings generation into a separate crate somewhere in the repo, that is not built by
    default. It should be built only if directly asked to build that crate.
  - Generate the bindings using that binary crate once, for both Python and Rust, and commit them
    into the repo. Rust bindings go directly into the src/bindings.rs instead of the OUT_DIR as they
    do now.
  - Add a test to the CI, that runs the bindings generator binary, and asserts the diff is empty.
  - Might be a good idea to include a timestamp in the generated code. If I end up doing that, I
    should account for that when checking the diff in CI.

  <2026-08-12 Wed 15:32> Baked the Rust bindings into the git, and pulled the build.rs logic out
  into a separate crate. Adding command line arguments etc. to the crate.

  <2026-08-12 Wed 15:35> Adding ~--check~ options to both Rust and Python bindings generation.

  - [X] Add the --check option to the CI to ensure the bindings never go out of sync.

  <2026-08-12 Wed 16:11> Added the checks to the CI. Not consistently passing on Windows. Started
  using the ~diff~ crate to see what the problem is.

* DONE tjpyye - Multithreading in a Python host
  CLOSED: [2026-08-13 Thu 15:28]
  :PROPERTIES:
  :CREATED:  2026-08-13 Thu 10:06
  :ARCHIVE_TIME: 2026-08-14 Fri 12:37
  :ARCHIVE_FILE: ~/dev/orc/PROJECT.org
  :ARCHIVE_CATEGORY: PROJECT
  :ARCHIVE_TODO: DONE
  :END:
  :LOGBOOK:
  - State "DONE"       from "NEXT"       [2026-08-13 Thu 15:28]
  - State "NEXT"       from "TODO"       [2026-08-13 Thu 13:53]
  :END:

  Need a proof of concept for a plugin function that uses multiple threads to do some work. It
  should also be able to check for cancellations, report messages, progress etc. from multiple
  threads. I was warned about problems with the Python interpreter, something called the GIL getting
  in my way. Need to navigate these problems, make the necessary design changes, and get it working.

  <2026-08-13 Thu 15:26> Works well. Had to overcome two minor issues:
  - The Python allocator was not respecting the requested alignment. Rayon wasn't happy with
    that. But worked after Python allocator was fixed.
  - Inside the report_message callback inside Python, I had to do ~flush=True~. Otherwise, Python
    printed weird garbage, because it was deferring the printing somehow and other threads were
    corrupting the data.

  Read to merge.

* DONE tjrpd8 - Support const generics in orc_fn macro
  CLOSED: [2026-08-14 Fri 12:25]
  :PROPERTIES:
  :CREATED:  2026-08-14 Fri 12:19
  :ARCHIVE_TIME: 2026-08-17 Mon 15:18
  :ARCHIVE_FILE: d:/dev/orc/PROJECT.org
  :ARCHIVE_CATEGORY: PROJECT
  :ARCHIVE_TODO: DONE
  :END:
  :LOGBOOK:
  - State "DONE"       from "NEXT"       [2026-08-14 Fri 12:25]
  - State "NEXT"       from "TODO"       [2026-08-14 Fri 12:20]
  :END:

  This was supposed to work. But turns out the generated code doesn't specify all the generics
  explicitly, and that is an issue. It needs to.

  <2026-08-14 Fri 12:24> LLM fixed it. The function below now compiles and runs without issues:
  #+begin_src rust
    #[orc_fn]
    fn sub() {
        let host_callbacks = host_callbacks();
        let registry: &ObjectRegistry = registry();
        let types = (run::<4, f32>, run::<8, f64>);

        /// Subtracts the second operand from the first, and assigns to the output. The input types must
        /// be the same, matching the output type. This function supports floating point scalar types.
        fn run<const N: usize, T>(lhs: &T, rhs: &T, out: &mut T)
        where
            T: TOrcData + Sub<Output = T> + Copy,
        {
            const {
                assert!(
                    N == std::mem::size_of::<T>(),
                    "Forcing the caller to pass in the size of the type T as a generic, to demonstrate the use of const generics."
                )
            };
            ,*out = *lhs - *rhs;
        }
    }
  #+end_src

  Read to merge.

* DONE tjockq - DAG capability inside Rust SDK
  CLOSED: [2026-08-15 Sat 14:28]
  :PROPERTIES:
  :CREATED:  2026-08-12 Wed 16:50
  :ARCHIVE_TIME: 2026-08-17 Mon 15:18
  :ARCHIVE_FILE: d:/dev/orc/PROJECT.org
  :ARCHIVE_CATEGORY: PROJECT
  :ARCHIVE_TODO: DONE
  :END:
  :LOGBOOK:
  - State "DONE"       from "NEXT"       [2026-08-15 Sat 14:28]
  - State "NEXT"       from "TODO"       [2026-08-12 Wed 16:50]
  :END:

  Currently inside kbb_cli_host. But more general DAG capabilities are useful in many plugins and
  host apps.

  <2026-08-12 Wed 18:33>  Moved the dag helper macro into the SDK. A DAG datastructure inside the
  SDK will require some more thought. It needs to be a generic graph to allow for cheap and
  arbitrary edits. A flat tape is not good for arbitrary edits. I want to resurrect the halfedge
  style graph structure I used in ~galproject~ many years ago.

  The datastructure should have a property system. A node editor might use it to save user assigned
  aliases / names for nodes. Another UI app might choose to store colors, render settings etc. This
  information should be stored in the file, as generic properties. The properties that one UI app
  cares about should not step on the toes of other UI apps. They should coexist with unique property
  identifiers.

  <2026-08-13 Thu 13:45> Trying to get the current state merged to pivot to a different task. Will
  return to this task later.

  <2026-08-13 Thu 19:26> Added a lot of untesed, unused boiler plate. The property system stuff is
  copied over from the alum project, with slight changes. Don't yet know what the outward facing API
  of a workflow will be. I need to think about how this will be used, and come up with something.

  <2026-08-15 Sat 08:02> Finalized the DAG implementation, with a property system and all, with a
  bit of testing. Next: parse kbb file into this DAG, and sketch out a way to run it in topological
  order.

  <2026-08-15 Sat 11:16> On second thought, I will defer the parsing from a file to a later task. I
  will just focus on running the DAG now.

  <2026-08-15 Sat 14:27> Running into lots of small road blocks. Trying to get the current state
  merged. Created separate tasks for everything else.

** CLOSED Check for cycles when adding new links
   CLOSED: [2026-08-14 Fri 14:12]
   :PROPERTIES:
   :CREATED:  2026-08-13 Thu 17:07
   :END:
   :LOGBOOK:
   - State "CLOSED"     from "TODO"       [2026-08-14 Fri 14:12]
   :END:

   The check should go [[file:rust/orc_sdk/src/dag.rs::pub fn push_link(&mut self, from: OH, to: IH) -> Option<LH> {][here]].

   <2026-08-14 Fri 13:32> I decided against doing this at this time. I will do what Grasshopper
   does, and allow the creation of graphs with cycles, and only perform the check once just before I
   have to run the graph.

   I think that would be way better than checking each link. Checking each link will make parsing
   a file and constructing a graph slow. Checking for one or more cycles in a complete graph is much
   cheaper than checking before each edit. So it makes sense to keep the cycle checking a concept
   associated with running the graph. When not running the graph, it is allowed to exist with cycles
   in an error state. This also allows the user to work save, reopen and edit files that may have
   turned cyclic by accident.

   Just a note for future development: I probably need some sort of "plan" step, that walks the DAG,
   determines what needs to be computed, and in what order. This phase might also do some sort of
   thread allocation, to figure out what parts of the DAG can be run concurrently. This phase should
   support a fully unevaluated DAG (e.g. after loading a new file), or a partially evaluated DAG
   (e.g. when making incremental edits to an active DAG, or loading cached outputs from disk). This
   "plan" phase takes on the responsibility of checking for cycles. It is also probably a good idea
   to run the garbage collection on the graph just before the plan phase. It seems like the best
   time to clean up after the user makes a long series of edits, and now wants to run the graph.

   In light of this insight, closing this task.


* DONE tjtsc1 - OrcHandle ownership in Rust
  CLOSED: [2026-08-15 Sat 17:37]
  :PROPERTIES:
  :CREATED:  2026-08-15 Sat 14:21
  :ARCHIVE_TIME: 2026-08-17 Mon 15:18
  :ARCHIVE_FILE: d:/dev/orc/PROJECT.org
  :ARCHIVE_CATEGORY: PROJECT
  :ARCHIVE_TODO: DONE
  :END:
  :LOGBOOK:
  - State "DONE"       from "NEXT"       [2026-08-15 Sat 17:37]
  - State "NEXT"       from "TODO"       [2026-08-15 Sat 15:19]
  :END:

  Currently, I am trying to do RAII for these handles. But at the same time, I need to make copies
  of these handles to pass as inputs to other functions. So right now I have a hacky
  ~non_owning_clone~ letting me do that. This is not good, and is causing more problems as I flesh
  out the details of the DAG implementation. I need something better. Ideally, I can just clone the
  handles as I wish, and they are automatically reference counted in the background for me. Or maybe
  I shouldn't use RAII at all for these handles, and have the host manually free them, and implement
  clone on them. This requires a lot more thought, and this needs to be figured out soon, before I
  can make more progress on other tasks.

  <2026-08-15 Sat 17:29> Got rid of the unsafe non_owning_clone. Now, a bitwise identical non
  owning, borrow checked wrapper class is used in its place. Tested it. Borrow checking
  works. Passing a pointer to the wrapper across the FFI boundary also works. Also implemented
  contant node type, unused, untested.

  <2026-08-15 Sat 17:37> Renamed variable to 'parameter' to make it less confusing. Getting this
  merged.


* DONE tjtyz1 - Running a DAG workflow
  CLOSED: [2026-08-16 Sun 17:03]
  :PROPERTIES:
  :CREATED:  2026-08-15 Sat 14:26
  :ARCHIVE_TIME: 2026-08-21 Fri 23:41
  :ARCHIVE_FILE: ~/dev/orc/PROJECT.org
  :ARCHIVE_CATEGORY: PROJECT
  :ARCHIVE_TODO: DONE
  :END:
  :LOGBOOK:
  - State "DONE"       from "NEXT"       [2026-08-16 Sun 17:03]
  - State "NEXT"       from "TODO"       [2026-08-15 Sat 17:42]
  :END:

  First I need to iron out the details of handle ownership in Rust, and figure out what are all the
  types of nodes I need in the DAG other than pure functions (constants, variables, ...). I need
  clarity on all that before I can start.

  <2026-08-15 Sat 17:45> Renamed the existing macro to ~orc_inline_dag~ to be more descriptive and
  avoid conflict with new macro I want to add, to construct a DAG for testing.

  <2026-08-15 Sat 19:05> Renamed ~orc_dag~ macro to ~orc_inline_dag~. Added support for ~const~ and
  multiple return values to the orc_inline_dag macro. Added a new ~orc_dag~ macro that constructs a
  DAG workflow instead of running the logic inline. This new macro hasn't been tested yet. I need a
  rudimentary way to walk and evaluate the macro to start testing it. I could generate topological
  tests, but feels messy. Seems much better to just write an evaluator and run that.

  <2026-08-16 Sun 16:25> Sketched out a depth first euler tour to walk and evaluate the DAG
  serially. This is good enough for testing, for now. Starting to write tests now.

  - [X] Use the ~const~ syntax in all the existing orc_inline_dag tests, in kbb_cli_host/src/test.rs
    instead of declaring the handles directly on the stack.

  <2026-08-16 Sun 16:43> All tests are passing. Now able to run DAGs, retrieve outputs, and test
  them. Checking a few more things, and ready to merge this soon.

  <2026-08-16 Sun 17:02> Decided to do the Python DAG construction as a separate task. Ready to
  merge.

* DONE tjw1vf - Nested DAG support
  CLOSED: [2026-08-19 Wed 18:54]
  :PROPERTIES:
  :CREATED:  2026-08-15 Sat 07:52
  :ARCHIVE_TIME: 2026-08-21 Fri 23:41
  :ARCHIVE_FILE: ~/dev/orc/PROJECT.org
  :ARCHIVE_CATEGORY: PROJECT
  :ARCHIVE_TODO: DONE
  :END:
  :LOGBOOK:
  - State "DONE"       from "NEXT"       [2026-08-19 Wed 18:54]
  - State "NEXT"       from "TODO"       [2026-08-16 Sun 20:50]
  :END:

  kbb file is a LISP dialect. The implementation goes [[file:rust/orc_sdk/src/dag/io.rs::pub fn parse_from_orc_file<P: AsRef<Path>>(path: P) -> Result<Self, DagError> {][here]].

  S-expressions represent a DAG. It should parse the same cases as the ~orc_inline_dag~ macro
  defined [[file:rust/orc_sdk/src/dag/mod.rs::macro_rules! orc_inline_dag {][here]]. And the ~orc_dag~ macro defined [[file:rust/orc_sdk/src/dag/mod.rs::macro_rules! orc_dag {][here]]. This should work like ~orc_dag~, except it
  should parse the syntax from a file. Need to write a fast and efficient lexer.

  Few ways in which this file format differs from the macros:

  - Each argument of a function call can optionally be prefixed by a label such as:

    #+begin_src lisp
      (func :label arg1 arg2)
    #+end_src

    This is purely cosmetic, for readability. These labels should be parsed and stored in an input
    property defined in the workflow. When a let binding is used for one or more outputs of a call,
    that should be parsed and stored in the output_labels property. These properties are located [[file:rust/orc_sdk/src/dag/workflow.rs::input_labels:
     InputProperty<String>,][here]].

  - The datatypes of const literals needs to be parsed. If the literals mention it as 1.23f32, or
    1.23f64, or 123u32, etc. They should be parsed to constants of that type. If the datatype is not
    specified in this way, we should still support integers (i64 by default), floats (f64 by
    default), and strings (u8 deck where each string is represented as a list of u8s). Strings will
    be declared with quotes "my_string".

  <2026-08-18 Tue 18:19> I am not sure if parsing a LISP syntax is the way to go. I am using this
  task to answer other, related, and more important questions. Those answers are making the permise
  of this task a bit redundant. I will still try to get this new stuff merged, but maybe as a
  different task, and close this task.

  <2026-08-18 Tue 19:33> Repurposing this task to flesh out the details of the DAG, including nested
  DAGs, and serialization of the DAG. Next I want to figure out representing and running nested
  workflows.

  <2026-08-19 Wed 16:04> Tried out the named arguments idea. Didn't like it. It was throwing off the
  Rust's syntax highlighting. Now, I almost have nested workflows working. Adding tests for
  that. Once the nested workflows are fully working, I will be in a position to serialize a
  hierarchy of workflows.

  <2026-08-19 Wed 18:51> Repurposed this task yet again. Nested DAGs are working, and I want to get
  this merged, and deal with serialization in a different task. Marking this done and getting it
  merged.

* DONE tk1hl6 - ABI - Return error codes from plugin functions
  CLOSED: [2026-08-19 Wed 19:38]
  :PROPERTIES:
  :CREATED:  2026-08-19 Wed 08:39
  :ARCHIVE_TIME: 2026-08-21 Fri 23:41
  :ARCHIVE_FILE: ~/dev/orc/PROJECT.org
  :ARCHIVE_CATEGORY: PROJECT
  :ARCHIVE_TODO: DONE
  :END:
  :LOGBOOK:
  - State "DONE"       from "NEXT"       [2026-08-19 Wed 19:38]
  - State "NEXT"       from "TODO"       [2026-08-19 Wed 19:09]
  :END:

  This allows the host program to handle known error scenarios in addition to just reporting strings
  to the user.

  <2026-08-19 Wed 19:37> Quite straight forward refactor. All the tests are passing. Getting this
  merged.

* DONE tk1ji0 - ABI - Serializing OrcHandle
  CLOSED: [2026-08-21 Fri 21:16]
  :PROPERTIES:
  :CREATED:  2026-08-19 Wed 18:38
  :ARCHIVE_TIME: 2026-08-25 Tue 18:59
  :ARCHIVE_FILE: d:/dev/orc/PROJECT.org
  :ARCHIVE_CATEGORY: PROJECT
  :ARCHIVE_TODO: DONE
  :END:
  :LOGBOOK:
  - State "DONE"       from "NEXT"       [2026-08-21 Fri 21:16]
  - State "NEXT"       from "TODO"       [2026-08-19 Wed 19:52]
  :END:

  For types that are owned by the plugin, the plugin alone can serialize and deserialize. The ABI
  needs to be extended to add support for this.

  <2026-08-19 Wed 20:02> Have the basic ABI in place. The design feels solid.

  <2026-08-20 Thu 17:00> Started implementing the serialization. Experimented with some weird plain
  text file formats for storing the OrcHandle's header (all the structural and metadata) in a format
  that all hosts and plugins can read and write. But then realized I don't need yet another format
  that everyone agrees on. I already have the ABI, so I can just write out the bytes compatible with
  the ABI, for the structural information. For the actual data:
  - Primitive types are just raw byte dumps. This is safe since the entire run time data sharing
    across hosts and plugins already relies on this being stable, and it is stable.
  - Custom types are deferred to the plugin that owns them, they are serialized to the end of the
    binary, after all the metadata in the header.

  <2026-08-21 Fri 20:17> Finally got everything mostly working. The handles are now being serialized
  and deserialized per their ABI. I have test coverage for most parts on their own, just need to add
  some more test coverage for the full serialization machiner from the host side. Need to do it from
  both the Rust and Python hosts.

  - [X] Add tests in the Rust host for the full roundtrip serialization of all types.
  - [X] Bring the Python host up to speed with all the serialization stuff, when it loads the
    plugins etc. and add tests for serialization.

  <2026-08-21 Fri 21:16> Python is all caught up with serialization, and everything is tested. Ready
  to merge.

** Ideas for host side context handling
   :PROPERTIES:
   :CREATED:  2026-08-19 Wed 20:05
   :END:

   The host needs to maintain per-call state (e.g. serialize buffers, progress tracking) that
   callbacks can access using the =ctx= token. Use a global concurrent registry keyed by =ctx=, same
   pattern as =DeckRegistry= (Rust) and the C SDK's handle registry. Each callback looks up =ctx= in
   the map to find its associated state.

   Thread-local storage is tempting but breaks under re-entrancy (=create_deck_from_proxy=
   dispatches host -> plugin A -> host -> plugin B) and plugin-spawned worker threads calling
   callbacks from threads the host didn't set up. A global map keyed by =ctx= handles both cases.

   Lock overhead per callback is negligible (~25ns uncontended). Plugins should batch writes into
   larger chunks anyway to minimize FFI crossings.

   This should also be the general pattern for how the host handles other callbacks such as progress
   reporting, etc.

* DONE tk5iyg - Serializing workflows
  CLOSED: [2026-08-22 Sat 16:58]
  :PROPERTIES:
  :CREATED:  2026-08-19 Wed 18:54
  :ARCHIVE_TIME: 2026-08-25 Tue 18:59
  :ARCHIVE_FILE: d:/dev/orc/PROJECT.org
  :ARCHIVE_CATEGORY: PROJECT
  :ARCHIVE_TODO: DONE
  :END:
  :LOGBOOK:
  - State "DONE"       from "NEXT"       [2026-08-22 Sat 16:58]
  - State "NEXT"       from "TODO"       [2026-08-21 Fri 23:29]
  :END:

  [[*Serializing OrcHandle][Serializing OrcHandle]] needs to be solved before this can be properly executed. I workshopped a
  vague, obviously untested plan with the LLM's help. Just reprsents a vague direction.

  <2026-08-22 Sat 13:21> Scrapping the plan to save the DAG as plain text. Supporting mutliple
  scripting languages already scratches the plain text itch, and I can revisit this decision later
  if need be. For now, I am going forward with message pack. It is easy to implement version
  support, is available in almost every language.

  <2026-08-22 Sat 14:31> Need to figure out how storing constants will affect the design of Python
  interop of the DAG workflows. Have some ideas, but not full clarity yet.

  <2026-08-22 Sat 16:58> Used message pack. Got everything working. Tested the rountrip
  serialization of workflows.

** CLOSED Vague Plan
   CLOSED: [2026-08-22 Sat 13:21]
   :PROPERTIES:
   :CREATED:  2026-08-19 Wed 18:55
   :END:
   :LOGBOOK:
   - State "CLOSED"     from              [2026-08-22 Sat 13:21]
   :END:

*** Overview

    Plain text, SSA-style pseudocode format. No external dependencies. The file is human readable and
    editable. A loaded ~PluginSet~ is required at parse time to resolve function names to
    ~FuncInfo~. Fields like ~FuncInfo.desc~, ~FuncInfo.func~, ~FuncInfo.n_inputs~, ~FuncInfo.n_outputs~
    are all recovered from the ~PluginSet~ — they are not serialized.

*** Reserved Words

    All keywords are prefixed with a dot: ~.workflow~, ~.const~, ~.return~. Property kind tags are
    also dot-prefixed: ~.input~, ~.output~, ~.node~, ~.link~. User-supplied names (function names,
    labels, workflow names, parameter names) must not start with a period. This avoids any collision
    between keywords and identifiers.

*** Workflow Declaration

    #+begin_example
    .workflow main(x, y):
      ...
    #+end_example

    - ~.workflow~ keyword, followed by the workflow name, followed by input parameter names in
      parentheses, followed by a colon.
    - Input parameter order in the signature is preserved across round trips.
    - Empty parentheses ~()~ for workflows with no inputs.
    - Everything inside a workflow is indented relative to the ~.workflow~ line.

*** Node Lines

    Nodes are listed one per line inside a workflow. Node indices are implicit from position (first
    line is node 0, second is node 1, etc.), like OBJ vertex indices.

    Output indices are global across all nodes within a workflow, assigned implicitly in declaration
    order.

**** Function nodes

    #+begin_example
    2 = add $x 0
    3 4 = sum_prod 1 2
    #+end_example

    - LHS lists the global output indices produced by this node.
    - RHS is the function name followed by arguments.
    - Arguments are either global output indices (bare integers) or workflow input references
      (~$name~).
    - ~_~ means an unconnected input.

**** Constant nodes

    #+begin_example
    0 = .const "offset"
    1 = .const
    #+end_example

    - ~.const~ keyword followed by an optional name string.
    - The optional name string corresponds to the ~name~ field of ~NodeInfo::Constant~. It is
      only written when non-empty.
    - Constant data is stored in a sibling binary file using MessagePack format. For a workflow
      file named ~foo.orc~, the constants file is ~foo.const~.
    - The ~.const~ file contains a msgpack array. Each entry is an arbitrary-length byte array
      containing serialized constant data. The plugins are responsible for deserializing each
      byte array.
    - Constants are stored in the array in the same order they appear in the workflow file.

**** Nested workflow calls

    #+begin_example
    5 = double 2
    #+end_example

    Syntactically identical to function calls. The parser disambiguates by checking the
    ~PluginSet~ first; if no function matches, it checks the workflow's nested workflows. This
    mirrors the runtime behavior and the ~orc_dag!~ macro's fallback logic.

**** Nodes with zero outputs

    #+begin_example
    fire_missiles 3
    #+end_example

    No LHS, no ~=~ sign. Just the function name and its arguments.

*** Return Statement

    #+begin_example
    .return 4 "result"
    .return 3 "sum" 4 "product"
    #+end_example

    - Lists the workflow's output(s) by global output index, optionally followed by a name string.
    - Names correspond to the ~String~ in ~workflow_outputs: Vec<(OH, String)>~.
    - Names are only written when non-empty.
    - Omit ~.return~ entirely for workflows with no outputs.

*** Properties

    Properties are nested inside each workflow, after the ~.return~ line. Each property block is a
    contiguous section. All values of one property are written together — they are not scattered as
    inline annotations on node lines.

    #+begin_example
    input_labels .input
      2:0 "lhs"
      2:1 "rhs"

    output_labels .output
      0 "sum"

    comments .node
      2 "add the constants"
    #+end_example

    - The property name comes first, then the property kind tag (~.input~, ~.output~, ~.node~,
      ~.link~).
    - Input properties are keyed by ~node_index:input_index~ (per-node input index, zero-based).
    - Output properties are keyed by global output index.
    - Node properties are keyed by node index.
    - Link properties are keyed by ~output_index:input_node:input_index~.
    - Only non-default values are written. Default values (empty strings) are omitted.

*** String Escaping

    - Names of nested workflows, functions, labels of nodes, constants etc. cannot contain double or
      single quotes. They also cannot start with a period.
    - Bare strings (identifiers like function names, workflow names, parameter names) are literal.
      They must not contain whitespace or special characters, and must not start with a period.
    - Quoted strings (labels, comments, names) use backslash escaping: ~\"~ for a literal quote,
      ~\\~ for a literal backslash, ~\n~ for newline.

*** Nested Workflows

    Nested workflows are indented inside their parent workflow, after the ~.return~ statement and
    properties, separated by an empty line for legibility.

    #+begin_example
    .workflow main(x):
      0 = .const [3.0f64]
      1 = add $x 0
      .return 1

      comments .node
        1 "offset by input"

      .workflow double(x):
        0 = mul $x $x
        .return 0

      .workflow triple(x):
        0 = mul $x $x
        1 = mul 0 $x
        .return 1

        .workflow helper(a):
          0 = add $a $a
          .return 0
    #+end_example

    - Nesting is conveyed by indentation level, mirroring the ~nested_workflows: HashMap<String,
      Workflow>~ hierarchy in the Rust struct.
    - Two workflows at different hierarchical paths can share the same name without conflict.
    - A workflow must be defined before it is called (definition appears before the call site in
      the node list, or as a nested workflow of an ancestor).

*** What Is NOT Serialized

    - ~FuncInfo.func~ — function pointer, resolved from ~PluginSet~ at load time.
    - ~FuncInfo.desc~ — recovered from ~PluginSet~.
    - ~FuncInfo.n_inputs~ / ~FuncInfo.n_outputs~ — recovered from ~PluginSet~.
    - ~OrcHandle.handle~ — runtime handle ID, reassigned by host at load time.
    - ~OrcHandle.free_fn~ — destructor, set by the allocating plugin at runtime.
    - ~OrcHandle.dims~ — units metadata. TODO: decide if this should be serialized.


* DONE tjvt98 - Constructing a DAG from Python
  CLOSED: [2026-08-25 Tue 16:51]
  :PROPERTIES:
  :CREATED:  2026-08-16 Sun 14:40
  :ARCHIVE_TIME: 2026-08-25 Tue 18:59
  :ARCHIVE_FILE: d:/dev/orc/PROJECT.org
  :ARCHIVE_CATEGORY: PROJECT
  :ARCHIVE_TODO: DONE
  :END:
  :LOGBOOK:
  - State "DONE"       from "NEXT"       [2026-08-25 Tue 16:51]
  - State "NEXT"       from "TODO"       [2026-08-23 Sun 14:21]
  :END:

  Ideally the same scripts used to run the plugin functions, can be repurposed by running them in a
  special context, that constructs the DAG instead of immediately doing the work.

  This is potentially a way of overcoming the overhead of calling functions repeatedly with ctypes
  over FFI. Instead the Python user may choose to construct the DAG and run it many times without
  the Python's ctypes function call overhead.

  <2026-08-23 Sun 14:26> After some more thought, I think it might be better to consolidate all the
  Python integration into a single crate built with pyo3.

  <2026-08-23 Sun 23:34> The generated code works, but the quality is horrendous. Working through it
  slowly, learning the ins and outs of pyo3, and cleaning it all up.

  - [X] Implement Default for PlugiSet, that creates an empty set, and add function to append to
    it. This is useful when loading plugins in multiple passes, for whatever reason.

  <2026-08-24 Mon 00:39> Slowly reviewed and refactored lib.rs, it is now decent. Need to go through
  the remaining files in the same way.

  - [X] Fix all the gratuitous .unwraps and .expects.
  - [X] Consolidate the mutexes being locked to build a workflow. This will make deadlocks and
    mistakes less likely.

  <2026-08-25 Tue 16:51> Ready to merge.

** Plan using PyO3
   :PROPERTIES:
   :CREATED:  2026-08-23 Sun 14:29
   :END:

   Add a crate that compiles it's binary using PyO3, and acts as a host program written in
   Rust. Loading the librariers, generating a .pyi on startup when required, and running the user
   scripts by calling functions inside loaded plugins, are all done by this Rust host, via the
   Python bindings.

   The function calls return thin wrappers that contain an OrcHandle, and are reference counted by
   the Python interpreter. When these are dropped, the underlying deck is freed within the plugin
   via the free_fn callback.

   This library should also expose a method like ~make_graph~, that takes a Python function
   (lambda). And run it with a different set of function bindings, that construct a workflow DAG
   instead of running the plugin functions directly, and return a handle to the Workflow graph back
   to the Python script. Any foreign Python code within that function should run as usual. The
   'Rebinding' only affects the functions exposed by this crate. The python script can then choose
   to run said graph with any inputs. This can be potentially faster for repeated incremental runs,
   if only few inputs are changed. The DAG can skip rerunning the functions whose inputs have not
   changed. So, this can be faster than just running the Python bindings. The Python script can also
   choose to serialize the workflow to messsage pack via another binding (uses orc_sdk's
   serialization under the hood), to open and run the same workflow in other tools. Similarly, the
   python script can deserialize a graph, that was exported in another tool, and run it with
   inputs. Basically, the plugin functions can be run either in 'immediate mode', or a deferred DAG
   evaluation mode.

   This new design should:
   - Allow the plugin functions to create threads freely, and report progress, messages, and other
     things from these threads. GIL should not cause complications.
   - I want to maintain the zero copy numpy integration. Large arrays of numbers owned by my
     plugins, should be available to numpy, without copying, to be interpreted as arrays and
     matrices. The current design has this capability, and that should be maintained.
   - Ownership story of OrcHandle instances should be clear.
   - Performance and memory efficienct are paramount.
   - The Python module should feel idiomaticc for Python users, and should prioritize convenience.
   - The design should conform to Python's stable ABI, with as lower a floor as possible, and be
     compatible with all versions of Python. Recompiling a binary for every version of Python is
     just silly.

*** Implementation Plan

    This section is a detailed, self-contained plan for implementing the ~pyorc~ crate. It is
    intended for an agent or developer who may not have participated in the design discussion. All
    design decisions and constraints are captured here.

**** Crate Setup

     - Crate name: ~pyorc~. Location: ~rust/pyorc/~.
     - Python module name: ~orc~. Users write ~import orc~.
     - Build tool: ~maturin~. Add a ~pyproject.toml~ alongside the crate.
     - Dependencies: ~orc_sdk~ (path dep), ~pyo3~ with feature ~abi3-py37~ for Python stable ABI
       (Python 3.7+ floor). No ~rust-numpy~ dependency.
     - The crate produces a shared library (~.so~ / ~.dylib~ / ~.pyd~) that Python imports directly.
     - This crate replaces the existing ~python/orc.py~ + ~python/bindings.py~ ctypes approach. Once
       this crate is working and tested, the ctypes files can be removed.

**** Architecture — Global Statics

     Follow the same pattern as ~rust/kbb_cli_host/src/main.rs~. The PyO3 crate is a Rust host
     program that happens to be loaded by Python. It uses the same global statics:

     - ~static PLUGIN_SET: OnceLock<PluginSet>~ — initialized on ~load_plugins()~ call from Python.
       Using ~OnceLock~ (not ~LazyLock~) because the plugin directory is provided at runtime by the
       user. Must error if called twice.
     - ~static REGISTRY: LazyLock<DeckRegistry>~ — for host-allocated decks (constants, primitives
       created by ~make_deck~).
     - ~static HANDLE_COUNTER: AtomicU64~ — monotonically increasing handle IDs.
     - ~static SERIAL_CONTEXT_ARENA: LazyLock<ContextArena<Vec<u8>>>~ — scratch space for
       serialization callbacks.

     Host callbacks (~report_message~, ~serial_write~, ~host_create_proxy_deck~) should be
     implemented identically to ~kbb_cli_host~. Messages are printed directly from Rust using Rust
     printing facilities. No routing to Python loggers. The ~HOST~ const is constructed the same way
     as ~kbb_cli_host::HOST~.

**** Python-Facing Types

***** ~Handle~ (#[pyclass(name = "Handle")])

      Wraps an owned ~OrcHandle~. When Python's reference count drops to zero, the destructor calls
      ~free_fn~ on the inner handle (same semantics as the ~_handle_del~ function in the current
      ~python/orc.py~).

      Read-only properties exposed to Python:
      - ~type_id: int~
      - ~n_items: int~
      - ~item_size: int~
      - ~dims: list[int]~ (length 7)

      Zero-copy numpy integration via ~__array_interface__~:
      - Implement a ~__array_interface__~ property (Python ~@property~, returns a ~dict~).
      - The dict contains: ~"shape"~ (tuple), ~"typestr"~ (numpy dtype string like ~"<f8"~),
        ~"data"~ (tuple of ~(pointer_as_int, False)~), ~"version"~ (3).
      - Map ~type_id~ to numpy typestr: ~ORC_TYPE_U8 -> "|u1"~, ~ORC_TYPE_F64 -> "<f8"~, etc.
        Use little-endian (~<~) for multi-byte types, ~|~ for single-byte types.
      - When numpy calls ~np.asarray(handle)~, it reads ~__array_interface__~ and creates a
        zero-copy view. The numpy array holds a reference to the ~Handle~ Python object, preventing
        the handle from being freed while the array is alive.
      - This approach requires no C-level numpy API, works with stable ABI, and does not require
        numpy to be installed unless the user actually calls ~np.asarray()~.

      Reference for ~__array_interface__~:
      https://numpy.org/doc/stable/reference/arrays.interface.html

***** ~GraphNode~ (#[pyclass])

      Thin wrapper around ~OH~ (output handle index in the workflow being built during
      ~make_graph~). This is what deferred-mode function calls return instead of ~Handle~.

      - Not constructable from Python. Only created internally by deferred-mode function dispatch.
      - Can be passed as input to other deferred-mode function calls (builds edges in the DAG).
      - For multi-output functions, the deferred call returns a Python ~list~ of ~GraphNode~, which
        the user can unpack: ~r, i = orc.complex_get_parts(c)~.
      - If a user attempts to use a ~GraphNode~ as a real value (comparison, arithmetic, etc.),
        Python will raise a ~TypeError~ naturally since ~GraphNode~ doesn't implement those
        protocols. This is the intended behavior — the user's function cannot be turned into a graph
        if it inspects intermediate values.

***** ~Graph~ (#[pyclass])

      Wraps a ~Workflow~ (from ~orc_sdk::dag~). Produced by ~make_graph~.

      Methods:
      - ~run(*args, **kwargs) -> Handle | list[Handle]~: Execute the workflow.
        - Positional args are matched to the workflow inputs by position.
        - Keyword args are matched by name (the parameter names from the original function passed to
          ~make_graph~). Named args are sorted into positional order before calling Rust's
          ~Workflow::run~.
        - Mixed positional and keyword args are supported (standard Python calling convention).
        - All args must be ~Handle~ instances.
        - Returns a single ~Handle~ if the workflow has one output, or a ~list[Handle]~ if multiple.
        - The GIL should be released during the Rust-side ~Workflow::run~ call.
      - The ~Graph~ stores the parameter names (from ~inspect~) so that ~run~ can resolve keyword
        arguments.

**** Module-Level Functions

     These are registered as ~#[pyfunction]~ items in the ~#[pymodule]~:

     - ~load_plugins(search_dir: str)~: Loads all plugin shared libraries from the given directory.
       Calls ~orc_sdk::load_plugins(dir, &HOST)~ to get a ~PluginSet~. Stores it in the global
       ~PLUGIN_SET~. For each function in each loaded plugin, creates a callable Python object and
       sets it as an attribute on the ~orc~ module (so that ~orc.add~, ~orc.mul~, etc. become
       available). Also triggers ~.pyi~ stub regeneration if needed (see stub generation section).
       Errors if called more than once.

     - ~make_deck(data, type_id=None) -> Handle~: Creates an ~OrcHandle~ from Python data (numbers,
       lists, nested lists). Same semantics as ~python/orc.py::make_deck~. Detects the narrowest
       type from the leaf values if ~type_id~ is not specified. Supports nested lists (creates marks
       and strides). The resulting handle is allocated in the host's ~REGISTRY~.

     - ~read_deck(handle: Handle) -> object~: Reads values out of a ~Handle~, reconstructing nested
       structure from marks. Returns Python lists/numbers. Same semantics as
       ~python/orc.py::read_deck~.

     - ~make_graph(fn: Callable) -> Graph~: Builds a workflow DAG by running ~fn~ in deferred mode.
       See the dedicated section below for full details.

     - ~run_graph(graph: Graph, *args, **kwargs) -> Handle | list[Handle]~: Convenience wrapper,
       equivalent to ~graph.run(*args, **kwargs)~. Allows interleaving DAG execution with immediate
       mode calls without method syntax.

     - ~serialize_workflow(graph: Graph, path: str)~: Serializes the workflow to a msgpack file using
       ~orc_sdk~'s serialization (the ~io.rs~ code). Uses ~BufWriter<File>~ as the ~RmpWrite~
       target.

     - ~deserialize_workflow(path: str) -> Graph~: Deserializes a workflow from a msgpack file.
       Requires ~PLUGIN_SET~ to be initialized (for ~FuncInfo~ recovery). Uses ~BufReader<File>~ as
       the ~RmpRead~ source.

**** Plugin Function Callables

     When ~load_plugins~ iterates over the functions in the ~PluginSet~, it creates a callable
     Python object for each one and sets it as a module attribute. This callable object (implemented
     as a ~#[pyclass]~ with a ~__call__~ method) stores:
     - The function name.
     - The ~FuncInfo~ (or a reference/index into the ~PluginSet~).
     - ~n_inputs~ and ~n_outputs~ (with ~None~ for variadic, matching ~ORC_ARGS_VARIADIC~).

     The ~__call__~ method checks a global ~AtomicBool IN_GRAPH_MODE~ flag:

     *Immediate mode* (~IN_GRAPH_MODE == false~):
     - Validates input count against ~n_inputs~ (if not variadic).
     - Releases the GIL via ~Python::allow_threads~.
     - Allocates output ~OrcHandle~s with fresh handle IDs from ~HANDLE_COUNTER~.
     - Calls the function pointer directly (same as ~OrcFuncWrapper.__call__~ in ~python/orc.py~).
     - Wraps each output ~OrcHandle~ in a Python ~Handle~ object.
     - Returns a single ~Handle~ if ~n_outputs == 1~, or a ~list[Handle]~ otherwise.

     *Deferred mode* (~IN_GRAPH_MODE == true~):
     - All inputs must be ~GraphNode~ instances (not ~Handle~).
     - Adds a function node to the workflow being built (stored in a global ~Mutex<Option<Workflow>>~
       during ~make_graph~).
     - Connects input ~GraphNode~'s ~OH~ values to the new node's inputs via ~Workflow::connect~.
     - Returns a ~GraphNode~ wrapping the new node's output ~OH~ (or a ~list[GraphNode]~ for
       multi-output functions).

     The two-path dispatch via ~AtomicBool~ is simple, correct, and has negligible overhead (a single
     atomic load per call). No method swapping needed.

**** ~make_graph~ Implementation

     #+begin_src python
     # Python-visible API:
     graph = orc.make_graph(lambda x, y: orc.add(x, y))
     result = graph.run(handle_a, handle_b)
     #+end_src

     Rust-side implementation of ~make_graph(fn: PyObject) -> Graph~:

     1. Assert ~IN_GRAPH_MODE.compare_exchange(false, true)~ succeeds. If it fails, raise a Python
        ~RuntimeError~: ~make_graph cannot be called recursively~.
     2. Create a fresh ~Workflow::default()~ and store it in a global
        ~static BUILDING_WORKFLOW: Mutex<Option<Workflow>>~.
     3. Use Python's ~inspect.signature(fn)~ (from the standard library) to get the parameter names.
        For each parameter:
        - Create a constant node or a workflow input in the workflow (via ~Workflow::set_inputs~).
        - Wrap the corresponding ~OH~ in a ~GraphNode~.
     4. Call ~fn(*graph_nodes)~ from Python. During this call, all ~orc.*~ plugin function calls go
        through the deferred code path (because ~IN_GRAPH_MODE~ is true), building up the workflow.
     5. Inspect the return value of ~fn~:
        - Single ~GraphNode~: one workflow output.
        - Tuple or list of ~GraphNode~: multiple workflow outputs.
        - Set these as the workflow outputs via ~Workflow::set_outputs~.
     6. Take the ~Workflow~ out of ~BUILDING_WORKFLOW~, wrap it in a ~Graph~ Python object.
     7. Set ~IN_GRAPH_MODE~ back to ~false~.
     8. Steps 4-7 must be wrapped in a ~try/finally~ equivalent (or Rust drop guard) so that
        ~IN_GRAPH_MODE~ is always reset and ~BUILDING_WORKFLOW~ is always cleaned up, even if ~fn~
        raises an exception.
     9. Return the ~Graph~.

     The ~Graph~ object stores the parameter names alongside the ~Workflow~, so that ~graph.run~ can
     accept keyword arguments and map them to positional inputs.

**** ~make_deck~ in Deferred Mode

     When ~IN_GRAPH_MODE~ is true and ~make_deck~ is called, it should:
     1. Create the ~OrcHandle~ as usual (allocate in ~REGISTRY~).
     2. Add a constant node to the building workflow via ~Workflow::add_constant~, storing the handle.
     3. Return a ~GraphNode~ wrapping the constant's output ~OH~.

     This allows the same user code to work in both modes:
     #+begin_src python
     def my_pipeline(x):
         offset = orc.make_deck([1.0, 2.0, 3.0])
         return orc.add(x, offset)

     # Immediate:
     result = my_pipeline(some_handle)

     # Deferred:
     graph = orc.make_graph(my_pipeline)
     result = graph.run(some_handle)
     #+end_src

**** .pyi Stub Generation

     On every ~load_plugins~ call, check whether the existing ~.pyi~ stub file is up to date:

     1. Find the newest modified time among all loaded plugin shared library files.
     2. Compare against the modified time of the ~.pyi~ file (located alongside the ~orc~ module's
        ~.so~ file, named ~orc.pyi~).
     3. If the ~.pyi~ does not exist, or its mtime is older than the newest plugin mtime, regenerate.
     4. If the ~.pyi~ is newer, skip regeneration (cheap path).

     Stub content includes:
     #+begin_src python
     from typing import Callable

     class Handle:
         @property
         def type_id(self) -> int: ...
         @property
         def n_items(self) -> int: ...
         @property
         def item_size(self) -> int: ...
         @property
         def dims(self) -> list[int]: ...
         @property
         def __array_interface__(self) -> dict: ...

     class GraphNode: ...

     class Graph:
         def run(self, *args: Handle, **kwargs: Handle) -> Handle | list[Handle]: ...

     def load_plugins(search_dir: str) -> None: ...
     def make_deck(data: object, type_id: int | None = None) -> Handle: ...
     def read_deck(handle: Handle) -> object: ...
     def make_graph(fn: Callable) -> Graph: ...
     def run_graph(graph: Graph, *args: Handle, **kwargs: Handle) -> Handle | list[Handle]: ...
     def serialize_workflow(graph: Graph, path: str) -> None: ...
     def deserialize_workflow(path: str) -> Graph: ...

     # --- Plugin functions (auto-generated) ---
     def add(a: Handle, b: Handle) -> Handle: ...
     def mul(a: Handle, b: Handle) -> Handle: ...
     def complex_get_parts(c: Handle) -> list[Handle]: ...
     # ... etc.
     #+end_src

     Plugin functions with ~n_outputs == 1~ have return type ~Handle~. Functions with ~n_outputs > 1~
     have return type ~list[Handle]~. Variadic functions use ~*args: Handle~.

**** GIL and Threading

     - Release the GIL (~Python::allow_threads~) during all plugin function calls in immediate mode
       and during ~Workflow::run~ in DAG mode. Plugin functions are native Rust/C code and do not
       touch Python objects.
     - Host callbacks (~report_message~, ~report_progress~, etc.) print directly from Rust. They do
       NOT acquire the GIL or interact with Python objects. This means plugins can freely spawn
       threads and call callbacks from those threads without GIL complications.
     - Plugin developers are trusted to handle their own thread safety.

**** Ownership and Lifetime Rules

     - Every ~OrcHandle~ is owned by exactly one entity at a time: either a Python ~Handle~ object,
       or a ~Workflow~ constant node, or the host's ~REGISTRY~.
     - When a Python ~Handle~ is dropped (refcount hits zero), ~free_fn~ is called, which tells the
       owning plugin to free the backing deck.
     - Plugin function calls borrow inputs (~*const OrcHandle~) and produce fresh, independently
       owned outputs. There is no aliasing — outputs do not reference input memory. Dropping an
       input handle after a function call does not invalidate outputs.
     - In DAG mode, constants are owned by the ~Workflow~ inside the ~Graph~. Python does not hold
       ~Handle~ references to these constants. When the ~Graph~ is dropped, its constants are freed.
     - ~graph.run~ produces fresh output ~Handle~ objects owned by Python. These are independent of
       the ~Graph~ and can outlive it.

**** Error Handling

     - ~load_plugins~ raises ~RuntimeError~ if called more than once, or if plugin loading fails.
     - Plugin function calls that return non-zero ~OrcError~ raise ~RuntimeError~ with the error
       code.
     - ~make_graph~ raises ~RuntimeError~ if called recursively.
     - ~graph.run~ raises ~ValueError~ if argument count mismatches, ~TypeError~ if args are not
       ~Handle~, ~RuntimeError~ for DAG execution errors (cycles, missing functions, etc.).
     - ~serialize_workflow~ / ~deserialize_workflow~ raise ~IOError~ for file errors, ~RuntimeError~
       for format errors.

**** What Is NOT In Scope (for now)

     - Conditional or loop-based DAG construction. The function passed to ~make_graph~ must be
       straight-line code. If it tries to inspect a ~GraphNode~ value, Python will naturally raise
       ~TypeError~.
     - Parallel DAG execution. The existing depth-first eval in ~Workflow::run~ is used. Parallelized
       eval will be added to ~Workflow::run~ in the future, and will automatically benefit ~pyorc~
       without changes to the Python API.
     - Python SDK (writing plugins in Python). That is a separate task.

**** Example Usage

     #+begin_src python
     import orc
     import numpy as np

     # Load plugins from a directory.
     orc.load_plugins("/path/to/plugins")

     # --- Immediate mode ---
     a = orc.make_deck([1.0, 2.0, 3.0])
     b = orc.make_deck([10.0, 20.0, 30.0])
     c = orc.add(a, b)
     print(orc.read_deck(c))  # [11.0, 22.0, 33.0]

     # Zero-copy numpy view.
     arr = np.asarray(c)
     print(arr * 2)  # [22.0, 44.0, 66.0]

     # Multi-output function.
     comp = orc.create_complex(a, b)
     real, imag = orc.complex_get_parts(comp)

     # --- DAG construction ---
     def my_pipeline(x, y):
         offset = orc.make_deck([100.0])
         total = orc.add(x, y)
         return orc.add(total, offset)

     graph = orc.make_graph(my_pipeline)

     # Run the graph with actual handles.
     result = graph.run(a, b)
     print(orc.read_deck(result))

     # Same graph, different inputs. DAG can cache unchanged branches.
     result2 = graph.run(b, a)

     # Named arguments also work.
     result3 = graph.run(x=a, y=b)

     # Interleave immediate and DAG calls freely.
     doubled = orc.mul(result, orc.make_deck([2.0]))

     # Serialize / deserialize.
     orc.serialize_workflow(graph, "my_pipeline.mpk")
     graph2 = orc.deserialize_workflow("my_pipeline.mpk")
     result4 = graph2.run(a, b)
     #+end_src

**** Files to Create

     - ~rust/pyorc/Cargo.toml~
     - ~rust/pyorc/pyproject.toml~ (maturin config)
     - ~rust/pyorc/src/lib.rs~ (module definition, ~load_plugins~, ~make_deck~, ~read_deck~,
       ~make_graph~, ~serialize_workflow~, ~deserialize_workflow~, ~run_graph~)
     - ~rust/pyorc/src/handle.rs~ (~Handle~ pyclass, ~__array_interface__~ property, destructor)
     - ~rust/pyorc/src/host.rs~ (global statics, ~HOST~ const, callbacks — closely follows
       ~kbb_cli_host/src/main.rs~)
     - ~rust/pyorc/src/func.rs~ (plugin function callable pyclass, immediate/deferred dispatch)
     - ~rust/pyorc/src/graph.rs~ (~Graph~ and ~GraphNode~ pyclasses, ~make_graph~ implementation)
     - ~rust/pyorc/src/stubs.rs~ (~.pyi~ generation and staleness checking)

** CLOSED Vague Plan from LLM
   CLOSED: [2026-08-23 Sun 14:26]
   :PROPERTIES:
   :CREATED:  2026-08-16 Sun 17:33
   :END:
   :LOGBOOK:
   - State "CLOSED"     from              [2026-08-23 Sun 14:26]
   :END:

*** Goal

    The same Python scripts that call plugin functions in immediate mode can be run inside a context
    manager that constructs a DAG instead of executing. The user can then evaluate the DAG natively
    (without Python's ctypes per-call overhead), or serialize it to a file.

*** Dispatch via method swapping, not runtime checks

    To avoid the cost of checking a global flag on every call, the context manager swaps out the
    dispatch methods on entry and restores them on exit.

    The two dispatch points are:

    - ~OrcFuncWrapper.__call__~ on the class (affects all plugin function wrappers at once).
    - ~orc.make_deck~ at module level.

    In DAG mode, the swapped-in versions record nodes and edges in the graph instead of doing work.
    In normal mode, the original methods are in place and there is zero overhead — no flag, no
    branch, no import side-effect.

*** Separate file, no changes to orc.py

    All compiled-mode logic lives in a separate file (e.g. ~dag.py~). It imports ~orc~ and
    monkey-patches the dispatch points inside the context manager. ~orc.py~ never imports or
    references ~dag.py~. If the user never imports ~dag.py~, no trace of it exists in the system.

*** Native shared library for graph construction and evaluation

    The context manager's constructor takes the name or path of a shared library that it loads via
    ctypes. All recording calls (~add_constant~, ~add_function~, ~add_link~) and the ~run~ call are
    dispatched to this library. The ABI of that shared library is to be determined.

*** API sketch

    #+begin_src python
    import orc
    from dag import DagContext

    orc.load_plugins(search_dir)

    # ---------- Immediate mode (unchanged) ----------
    a = orc.make_deck([1.0, 2.0, 3.0])
    b = orc.make_deck([10.0, 20.0, 30.0])
    c = orc.add(a, b)
    print(orc.read_deck(c))

    # ---------- Compiled mode (same user code) ----------
    with DagContext("path/to/native_dag_lib.so") as g:
        a = orc.make_deck([1.0, 2.0, 3.0])
        b = orc.make_deck([10.0, 20.0, 30.0])
        c = orc.add(a, b)
        d = orc.mul(c, a)

        # Evaluate inside the context. Takes a list of proxy outputs to compute,
        # returns a corresponding list of real OrcHandles.
        results = g.run([c, d])

    # results are real OrcHandles that outlive the context.
    print(orc.read_deck(results[0]))
    print(orc.read_deck(results[1]))
    #+end_src

*** Serialization with ~g.write(path)~

    ~g.write(path)~ writes the entire DAG out to a file using an s-expression syntax with parity to
    the syntax used by the ~orc_dag!~ and ~orc_inline_dag!~ Rust macros. This file can later be
    loaded by a Rust program to display the graph in a node editor, re-evaluate it, etc.

*** Context manager internals

    #+begin_src python
    class DagContext:
        def __init__(self, lib_path):
            self._lib = ctypes.CDLL(lib_path)
            # ... set up argtypes/restype for the native API ...
            self._orig_call = None
            self._orig_make_deck = None

        def __enter__(self):
            self._orig_call = OrcFuncWrapper.__call__
            self._orig_make_deck = orc.make_deck

            ctx = self

            def recording_call(wrapper_self, *inputs, n_out=None):
                return ctx._add_function(wrapper_self, inputs, n_out)

            OrcFuncWrapper.__call__ = recording_call
            orc.make_deck = lambda data, type_id=None: ctx._recording_make_deck(data, type_id)
            return self

        def __exit__(self, *exc):
            OrcFuncWrapper.__call__ = self._orig_call
            orc.make_deck = self._orig_make_deck

        def _recording_make_deck(self, data, type_id=None):
            handle = self._orig_make_deck(data, type_id)
            return self._add_constant(handle)

        def _add_constant(self, handle):
            # Dispatch to native lib: register constant node, return proxy.
            ...

        def _add_function(self, wrapper, inputs, n_out):
            # Dispatch to native lib: register function node + edges, return proxy.
            ...

        def run(self, outputs):
            # Dispatch to native lib: evaluate the DAG for the requested outputs.
            # Returns a list of real OrcHandles.
            ...

        def write(self, path):
            # Serialize the entire DAG to the given file path.
            ...
    #+end_src

* DONE tkcjtv - Function metadata ABI improvements
  CLOSED: [2026-08-25 Tue 19:41]
  :PROPERTIES:
  :CREATED:  2026-08-15 Sat 13:54
  :ARCHIVE_TIME: 2026-08-26 Wed 16:15
  :ARCHIVE_FILE: d:/dev/orc/PROJECT.org
  :ARCHIVE_CATEGORY: PROJECT
  :ARCHIVE_TODO: DONE
  :END:
  :LOGBOOK:
  - State "DONE"       from "NEXT"       [2026-08-25 Tue 19:41]
  - State "NEXT"       from "TODO"       [2026-08-25 Tue 18:30]
  :END:

  A node-editor UI app that wants to create nodes with properly named inputs needs to get that
  information from the plugin that defines the corresponding function. This will require adding new
  fields to the function metadata's ABI. It certainly needs to be flexible, and allow for the host
  app to overwrite the behavior if need be.

  Requires more thought as to what the new metadata will be. I need to come up with a proper
  algebraic datatype that supports all the below cases. These apply for both inputs and outputs. I
  am using 'arguments' as a general term for both inputs and outputs:

  1. Fixed number of arguments with all their types known.
  2. Fixed number of arguments, with some of the types known.
  3. Fixed number of arguments, where none of the types are known.
  4. Variadic arguments, all of a single known type.
  5. Variadic arguments, where none of the types are known.

  Once this is implemented, and used, based on that experience it might be good to add more things
  to this metadata to improve the overall experience. But no matter what, it should remain purely
  metadata. The actual function should still handle everything procedurally. Should be completely
  optional. Everything should still work without this metadata, albeit a more inconvenient manner.

  <2026-08-20 Thu 19:54> I just realized another requirements UI apps will have. There should be a
  way to auto populate the names of arguments. And also autopopulate the names of newly added
  arguments for variadic blocks.

  <2026-08-20 Thu 20:22> Just had a decent idea. Two structs of the same kind. Host owns and write
  into one of them, and the plugin owns and writes into the other. They use this to communicate back
  and forth. Host says what it has, and plugin fills out the information it knows, or an error if
  what the host has is not valid. At the end of the dialogue, the host will call a free function to
  clean up the plugin's struct, and cleans up it's own. This is inspired by ~git credential fill~.

  <2026-08-25 Tue 13:30> When this changes, all host programs need to be updated to respect the new
  contract. Their input validation code will likely change.

  <2026-08-25 Tue 18:53> Started working on this task. I think I can make meaningful progress if I
  drop #4 in the above list. That is the most problematic case for a design that relies on static
  lists to communicate this metadata. And it is not that useful. Removing it lets me move faster,
  and I can revisit the design if I discover outliers, or if the "metadata as static list" design
  choice turns into a problem later.

  <2026-08-25 Tue 19:13> Added a simple name field for the arg info. Don't need descriptions for
  them. The function's description should take care of describing what the arguments mean in
  different scenarios. Updated the Rust macro to generate the new metadata based on argument names
  used in the Rust function.

  <2026-08-25 Tue 19:40> Took a few small steps in the right direction. But it hard to know if this
  task is moving in the right direction, and if I am choosing the right ABI for the metadata,
  without having anything that consumes the ABI, to test with. So I will revisit this topic if it
  turns into a problem when I build some demo apps. For now, trying to merge what I have in this
  task so far.


* DONE tkcg1m - Nested workflow support in Python generated DAG
  CLOSED: [2026-08-25 Tue 18:14]
  :PROPERTIES:
  :CREATED:  2026-08-16 Sun 14:36
  :ARCHIVE_TIME: 2026-08-26 Wed 16:15
  :ARCHIVE_FILE: d:/dev/orc/PROJECT.org
  :ARCHIVE_CATEGORY: PROJECT
  :ARCHIVE_TODO: DONE
  :END:
  :LOGBOOK:
  - State "DONE"       from "NEXT"       [2026-08-25 Tue 18:14]
  - State "NEXT"       from "TODO"       [2026-08-25 Tue 17:08]
  :END:

  Need a way to define incomplete fragments of a workflow DAG, and reuse those fragments by
  partially or fully substituting the incomplete parts.

  In a node editor UI app, this will map to something similar to Grasshopper's concept of
  "cluster". In a Python script, this is equivalent to definng functions, and currying them. In
  LISP, it is also equivalent to defining functions and currying them. I need to figure out a
  complete picture of this concept that includes how it is represented in memory, how it is drawn on
  screen in a UI app, and how it is stored on the disk in various formats, and make sure they all
  integrate seamlessly.

  <2026-08-16 Sun 22:46> For the Python part of the puzzle, using decorators to wrap Python
  functions is an interesting idea. This will allow me to map Python functions, call structure and
  nesting into the DAG representation. Going the other way is easy, it is just a code generation
  problem. The Python function decorator receives the function name, signature, argument names
  etc. All that can be transferred over into the DAG world and back.

  <2026-08-19 Wed 19:04> The general idea of running a script in "capture mode" to create a DAG
  generalizes for all scripting languages. This is nice. Much better way than parsing a language and
  converting it to a graph. Allows idiomatic programming in each language. The Graph can then be
  transpiled back into any scripting language (usual code gen).

  <2026-08-25 Tue 17:04> Repurposing this task in light of the other features I implemented. Changed
  title from 'Functions and Scripts - a clear picture' to 'Nested workflow support in Python
  generated DAG'.

  <2026-08-25 Tue 17:54> First pass seems to be working. Will do some more testing, and reviewing
  before trying to merge this.

  <2026-08-25 Tue 18:14> Testing as much as I can via unit tests for now. But the real test for this
  feature will be after I implement a visualization tool for the DAG. For now, this is ready to
  merge.

* DONE tkcg1m - Nested workflow support in Python generated DAG
  CLOSED: [2026-08-25 Tue 18:14]
  :PROPERTIES:
  :CREATED:  2026-08-16 Sun 14:36
  :ARCHIVE_TIME: 2026-09-04 Fri 16:22
  :ARCHIVE_FILE: ~/dev/orc/PROJECT.org
  :ARCHIVE_CATEGORY: PROJECT
  :ARCHIVE_TODO: DONE
  :END:
  :LOGBOOK:
  - State "DONE"       from "NEXT"       [2026-08-25 Tue 18:14]
  - State "NEXT"       from "TODO"       [2026-08-25 Tue 17:08]
  :END:

  Need a way to define incomplete fragments of a workflow DAG, and reuse those fragments by
  partially or fully substituting the incomplete parts.

  In a node editor UI app, this will map to something similar to Grasshopper's concept of
  "cluster". In a Python script, this is equivalent to definng functions, and currying them. In
  LISP, it is also equivalent to defining functions and currying them. I need to figure out a
  complete picture of this concept that includes how it is represented in memory, how it is drawn on
  screen in a UI app, and how it is stored on the disk in various formats, and make sure they all
  integrate seamlessly.

  <2026-08-16 Sun 22:46> For the Python part of the puzzle, using decorators to wrap Python
  functions is an interesting idea. This will allow me to map Python functions, call structure and
  nesting into the DAG representation. Going the other way is easy, it is just a code generation
  problem. The Python function decorator receives the function name, signature, argument names
  etc. All that can be transferred over into the DAG world and back.

  <2026-08-19 Wed 19:04> The general idea of running a script in "capture mode" to create a DAG
  generalizes for all scripting languages. This is nice. Much better way than parsing a language and
  converting it to a graph. Allows idiomatic programming in each language. The Graph can then be
  transpiled back into any scripting language (usual code gen).

  <2026-08-25 Tue 17:04> Repurposing this task in light of the other features I implemented. Changed
  title from 'Functions and Scripts - a clear picture' to 'Nested workflow support in Python
  generated DAG'.

  <2026-08-25 Tue 17:54> First pass seems to be working. Will do some more testing, and reviewing
  before trying to merge this.

  <2026-08-25 Tue 18:14> Testing as much as I can via unit tests for now. But the real test for this
  feature will be after I implement a visualization tool for the DAG. For now, this is ready to
  merge.

* DONE tkcjtv - Function metadata ABI improvements
  CLOSED: [2026-08-25 Tue 19:41]
  :PROPERTIES:
  :CREATED:  2026-08-15 Sat 13:54
  :ARCHIVE_TIME: 2026-09-04 Fri 16:22
  :ARCHIVE_FILE: ~/dev/orc/PROJECT.org
  :ARCHIVE_CATEGORY: PROJECT
  :ARCHIVE_TODO: DONE
  :END:
  :LOGBOOK:
  - State "DONE"       from "NEXT"       [2026-08-25 Tue 19:41]
  - State "NEXT"       from "TODO"       [2026-08-25 Tue 18:30]
  :END:

  A node-editor UI app that wants to create nodes with properly named inputs needs to get that
  information from the plugin that defines the corresponding function. This will require adding new
  fields to the function metadata's ABI. It certainly needs to be flexible, and allow for the host
  app to overwrite the behavior if need be.

  Requires more thought as to what the new metadata will be. I need to come up with a proper
  algebraic datatype that supports all the below cases. These apply for both inputs and outputs. I
  am using 'arguments' as a general term for both inputs and outputs:

  1. Fixed number of arguments with all their types known.
  2. Fixed number of arguments, with some of the types known.
  3. Fixed number of arguments, where none of the types are known.
  4. Variadic arguments, all of a single known type.
  5. Variadic arguments, where none of the types are known.

  Once this is implemented, and used, based on that experience it might be good to add more things
  to this metadata to improve the overall experience. But no matter what, it should remain purely
  metadata. The actual function should still handle everything procedurally. Should be completely
  optional. Everything should still work without this metadata, albeit a more inconvenient manner.

  <2026-08-20 Thu 19:54> I just realized another requirements UI apps will have. There should be a
  way to auto populate the names of arguments. And also autopopulate the names of newly added
  arguments for variadic blocks.

  <2026-08-20 Thu 20:22> Just had a decent idea. Two structs of the same kind. Host owns and write
  into one of them, and the plugin owns and writes into the other. They use this to communicate back
  and forth. Host says what it has, and plugin fills out the information it knows, or an error if
  what the host has is not valid. At the end of the dialogue, the host will call a free function to
  clean up the plugin's struct, and cleans up it's own. This is inspired by ~git credential fill~.

  <2026-08-25 Tue 13:30> When this changes, all host programs need to be updated to respect the new
  contract. Their input validation code will likely change.

  <2026-08-25 Tue 18:53> Started working on this task. I think I can make meaningful progress if I
  drop #4 in the above list. That is the most problematic case for a design that relies on static
  lists to communicate this metadata. And it is not that useful. Removing it lets me move faster,
  and I can revisit the design if I discover outliers, or if the "metadata as static list" design
  choice turns into a problem later.

  <2026-08-25 Tue 19:13> Added a simple name field for the arg info. Don't need descriptions for
  them. The function's description should take care of describing what the arguments mean in
  different scenarios. Updated the Rust macro to generate the new metadata based on argument names
  used in the Rust function.

  <2026-08-25 Tue 19:40> Took a few small steps in the right direction. But it hard to know if this
  task is moving in the right direction, and if I am choosing the right ABI for the metadata,
  without having anything that consumes the ABI, to test with. So I will revisit this topic if it
  turns into a problem when I build some demo apps. For now, trying to merge what I have in this
  task so far.

* DONE tke36o - Support and testing for optional inputs for plugin functions
  CLOSED: [2026-08-26 Wed 13:30]
  :PROPERTIES:
  :CREATED:  2026-08-25 Tue 20:04
  :ARCHIVE_TIME: 2026-09-04 Fri 16:22
  :ARCHIVE_FILE: ~/dev/orc/PROJECT.org
  :ARCHIVE_CATEGORY: PROJECT
  :ARCHIVE_TODO: DONE
  :END:
  :LOGBOOK:
  - State "DONE"       from "NEXT"       [2026-08-26 Wed 13:30]
  - State "NEXT"       from "TODO"       [2026-08-26 Wed 13:02]
  :END:

  In theory, the host/caller can simply pass emtpy orc handles, and let the function decide what
  they want to do. This should be easy to get working, just a bit tedious, and not super urgent.

  - Update ~Combinations~ to support and ignore empty handles.
  - The Rust proc macros should be updated to support Optional inputs.
  - Update all the host programs to not be very strict about the number of inputs.
  - Python host in pyorc will require some special attention to support sparse, named inputs.

  <2026-08-26 Wed 12:59> Reducing scope for this. Scrapping the above Combinations work. For now, I
  will just implement some leniency on the host side to call the plugin functions with empty
  handles. And I will implement strict checks on the plugin side. Tolerating empty inputs on the
  plugin side is not a priority at this time, and likely not for a while, and even later, it can be
  done without touching anything outside the plugin.

  <2026-08-26 Wed 13:24> Updated the immediate calls in the Python host, and updated the expected
 errors in tests.

 <2026-08-26 Wed 13:29> Now the graph mode also matches the behavior. This task is done.

 <2026-08-26 Wed 14:25> Also sneaking in a queue implementation into the C SDK that I copied over
 from another project. Will need it in another task soon.

*** CLOSED Vague LLM Plan
    CLOSED: [2026-08-26 Wed 13:59]
    :PROPERTIES:
    :CREATED:  2026-08-25 Tue 20:07
    :END:
    :LOGBOOK:
    - State "CLOSED"     from              [2026-08-26 Wed 13:59]
    :END:

    1. ~Combinations~: when iterating, treat empty handles as producing a single ~None~ item (depth
       irrelevant). Propagate this into ~ReadCursor~ (yields ~None~ when the underlying handle is
       empty) and ~DeckView~ (constructed as empty/absent).

    2. ~orc_sdk/derive_macros~: add ~optional: bool~ to ~ParamInfo~. Recognise ~Option<&T>~ in
       ~is_output_param~ / ~inner_type~ / ~infer_depth~ (restrict to depth-0 only; reject
       ~Option<&[T]>~ etc.). Skip the non-empty assertion for optional inputs. Exclude optional inputs
       from the type-dispatch scrutinee. In the generated ~dispatch_~ fn, pass all handles (including
       empty ones) to ~Combinations~; the updated ~Combinations~ handles them transparently. Optional
       call-args become ~Option<&T>~ drawn from the cursor's ~None~-or-~Some~ result.

    3. Host side: relax the strict input-count checks so callers can omit trailing optional inputs
       (pass fewer handles than ~n_inputs~; the remainder are treated as empty).

    4. ~pyorc~: support sparse / named inputs — allow passing a ~dict~ mapping parameter name →
       deck, leaving unmentioned optional inputs as empty handles.

    <2026-08-26 Wed 13:59> Decided not to do this right now.

* DONE tke7ks - Rename plugins to be more generic
  CLOSED: [2026-08-26 Wed 16:07]
  :PROPERTIES:
  :CREATED:  2026-08-26 Wed 16:00
  :ARCHIVE_TIME: 2026-09-04 Fri 16:22
  :ARCHIVE_FILE: ~/dev/orc/PROJECT.org
  :ARCHIVE_CATEGORY: PROJECT
  :ARCHIVE_TODO: DONE
  :END:
  :LOGBOOK:
  - State "DONE"       from "NEXT"       [2026-08-26 Wed 16:07]
  - State "NEXT"       from "TODO"       [2026-08-26 Wed 16:01]
  :END:

  math_plugin -> example_rust_plugin.
  deck_ops_plugin -> example_c_plugin.

  Because I want to add random blocks that make for interesting demos. Actual plugins will be built
  outside this project. The ones inside the project are just examples.

  <2026-08-26 Wed 16:07> Done.

* DONE tj45qx - C++ SDK - Deck implementation and helpers
  CLOSED: [2026-08-26 Wed 22:33]
  :PROPERTIES:
  :CREATED:  2026-08-01 Sat 18:58
  :ARCHIVE_TIME: 2026-09-04 Fri 16:22
  :ARCHIVE_FILE: ~/dev/orc/PROJECT.org
  :ARCHIVE_CATEGORY: PROJECT
  :ARCHIVE_TODO: DONE
  :END:
  :LOGBOOK:
  - State "DONE"       from "NEXT"       [2026-08-26 Wed 22:33]
  - State "NEXT"       from "DONE"       [2026-08-26 Wed 22:22]
  - State "DONE"       from "NEXT"       [2026-08-26 Wed 21:53]
  - State "NEXT"       from "TODO"       [2026-08-26 Wed 19:45]
  :END:

  LLMs can easily port the Deck implementation etc. But the abstractions to remove friction in
  implementing blocks will require some thought. I went with macros as the first pass in Rust. It is
  not clear to me what I should go with in C++.

  <2026-08-26 Wed 21:52> Setup a simple C++ static library, and a test executable, ported the error
  type, and the deck type, with some basic functionality, and testing. Will try to get this merged
  after I port the cursors, views, writers and combinations.

  <2026-08-26 Wed 22:22> Just ported the cursors, writers, views, and the corresponding test
  coverage.

  <2026-08-26 Wed 22:33> The porting story of ffi.rs is not fully clear in my head. I will put that
  off for later. Merging what I have so far.

* DONE tiwkbm - Server / client demo app
  CLOSED: [2026-09-04 Fri 15:33]
  :PROPERTIES:
  :CREATED:  2026-07-28 Tue 16:45
  :ARCHIVE_TIME: 2026-09-06 Sun 22:23
  :ARCHIVE_FILE: ~/dev/orc/PROJECT.org
  :ARCHIVE_CATEGORY: PROJECT
  :ARCHIVE_TODO: DONE
  :END:
  :LOGBOOK:
  - State "DONE"       from "NEXT"       [2026-09-04 Fri 15:33]
  - State "NEXT"       from "NEXT"       [2026-08-26 Wed 21:54]
  - State "NEXT"       from "TODO"       [2026-08-25 Tue 20:13]
  - State "TODO"       from "NEXT"       [2026-07-31 Fri 20:46]
  - State "NEXT"       from "TODO"       [2026-07-30 Thu 18:36]
  :END:

  Not that the plugin architecture is fleshed out, I can build a few toy host applications, and toy
  plugins, and demonstrate the flexibility and power of this design to both Product / Customer
  facing side as well as the engineering side.

  <2026-07-30 Thu 18:35> Made quite some progress with the orc_fn macro to make it easier to
  implement simple blocks. The macro is now able to handle templated Rust functions too, but still
  requires some more testing before I feel confident about it.

  <2026-07-31 Fri 18:22> The orc_fn proc macro mostly took shape. Trying to simplify the code a bit,
  and maybe add some tests. The proxy deck functions need more thought, and need to be fleshed
  out. Also the type ids require more thought. Plugins will need the ability to work with arbitrary
  types defined in other plugins. Which means plugins cannot rely on their own type ids. The handle
  cannot just communicate OrcTypeId. It needs to have a unique integer id that the host assigns to
  that type, after loading all the plugins, and the plugins need to know that mapped id for their
  own types after plugin_init.... It will get a bit more complicated than it currently is.

  <2026-07-31 Fri 20:45> Trying to temporarily stop on this task, and finish a few other smaller
  tasks before continuing.

  <2026-08-25 Tue 20:19> Last time I attempted this task, questions about type_ids, ownership, DAGs,
  versioning, and serialization were all completely unanswered. I have since answered those
  questions through all the tasks I finished during this time. Trying to attempt to make a UI app
  once again.

  <2026-08-27 Thu 17:45> The server client is working locally. Tried testing across machines on a
  local network. That did not work. Will try again later.

  - [X] Add integration tests for the client / server interactions.

  <2026-09-04 Fri 15:29> Server-client example seems to work over the local network. This is just an
  example app, and doesn't need to be very polished. I am now narrowing the scope of this task to
  just the server client demo app, and merging this. I will pull the other app ideas into separate
  tasks.

** DONE Host program that runs as a service
   CLOSED: [2026-09-04 Fri 15:31]
   :PROPERTIES:
   :CREATED:  2026-08-26 Wed 10:52
   :END:
   :LOGBOOK:
   - State "DONE"       from "TODO"       [2026-09-04 Fri 15:31]
   :END:

   Server / service that listens for requests, and performs work by calling plugin functions when it
   receives a request. This is interesting, as I can probably run a workflow on multiple threads
   using make, of course, assuming the host program can schedule the jobs it receives on all
   available threads.

   A client would:
   - Start a session, and receive a session id from the service. This helps the service distinguish
     the requests it receives from different clients in the future.
   - Create constants containing primitive datatypes, by passing them in as either binary blobs or
     strings. The server returns an id representing the constant that lives in the server's memory,
     associated with that particular sesion.
   - Call functions with specific inputs. The client specifies inputs by their ids. The server runs
     the funciton, and returns ids corresponding to the inputs.
   - Presumably some of these functions export files, that are received by the client as the output
     of their workflow.
   - Client can request to download the workflow they built up during this session as a serialized
     workflow.
   - The client can then close the session, which will cause all the session data, variables,
     registry etc. to be cleaned up.

   Comparing this to the host programs I have now, each session will have it's own OrcHost instance,
   registry, allocators, etc.

*** DONE Rust server plus C client
    CLOSED: [2026-09-04 Fri 15:31]
    :PROPERTIES:
    :CREATED:  2026-08-26 Wed 11:38
    :END:
    :LOGBOOK:
    - State "DONE"       from "TODO"       [2026-09-04 Fri 15:31]
    - State "TODO"       from              [2026-08-26 Wed 14:09]
    :END:

    Make a server in Rust: tiny_http crate provides a synchronous HTTP server — no async, no
    tokio. Thread pool is std-only: N threads pulling jobs from an mpsc channel. Plugin calls go
    through FFI into the C SDK, run on the thread pool, results returned as HTTP responses. Each
    session owns an OrcHost instance, stored in an Arc<Mutex<HashMap>>. Cross-platform via std and
    tiny_http.

    Make a simple client program in C:
    - It should be able to do all the client things listed in the parent task.
    - Needs a special --save-image <path> CLI option to save output data that is downloaded as
      serialized OrcHandle data. Say, if three handles (decks) of the same length are returned, save
      them as RGB. If 1 deck is returned, save it as grayscale.
    - Similar option (--save-text <path>) to save a downloaded serialized OrcHandle of type u8 as
      text to a file.
    - Should parse the numerical data passed in via CLI to create constant decks. Maybe the parsing
      can be done on the server side. Or maybe the client should create a deck, a handle, serialize
      it, then send it over to the server? Not sure which is better.

*** CLOSED C program using mongoose and a simple handrolled thread pool
    CLOSED: [2026-08-26 Wed 13:58]
    :PROPERTIES:
    :CREATED:  2026-08-26 Wed 11:38
    :END:
    :LOGBOOK:
    - State "CLOSED"     from              [2026-08-26 Wed 13:58]
    :END:

    Drop mongoose.c + mongoose.h into the repo. HTTP parsing, routing, and cross-platform socket
    handling are provided by mongoose. Thread pool is pthreads + a job queue (mutex + condvar).
    Plugin calls run directly through the C SDK with no FFI boundary. Each session owns an OrcHost
    instance, stored in a hashtable keyed by session id. Fully cross-platform (Windows, Linux,
    macOS).

    <2026-08-26 Wed 13:58> Replicating the workflow, its property system etc. in C is a good idea,
    but too much overhead for a quick demo. So I will scrap this idea and go ahead with the Rust
    alternative.

    <2026-08-26 Wed 14:10> Instead maybe, I can use mongoose to make a simple client program.

    <2026-09-04 Fri 15:32> Ended up not needing mongoose. Just a few hundren lines of boiler plate
    was enough.

* DONE tkuwku - Download workflow from the server
  CLOSED: [2026-09-04 Fri 17:17]
  :PROPERTIES:
  :CREATED:  2026-09-04 Fri 16:22
  :ARCHIVE_TIME: 2026-09-06 Sun 22:23
  :ARCHIVE_FILE: ~/dev/orc/PROJECT.org
  :ARCHIVE_CATEGORY: PROJECT
  :ARCHIVE_TODO: DONE
  :END:
  :LOGBOOK:
  - State "DONE"       from "NEXT"       [2026-09-04 Fri 17:17]
  - State "NEXT"       from "TODO"       [2026-09-04 Fri 16:22]
  :END:

  Add a ~download_workflow~ command to ~server_host~ that takes a session id and sends the
  serialized workflow as a .orc file.

  Add an integrateion test (to integration_tests.py), that creates a workflow with some basic math
  blocks, asserts the output value by downloading the output handle, then downloads the entire
  workflow and runs it locally via pyorc, and makes sure it runs properly.

  <2026-09-04 Fri 17:17> Got it working. This exposed an existing memory leak in pyorc. That is
  fixed. And the tests are passing.

* CLOSED Thread Safety in C SDK
  CLOSED: [2026-09-04 Fri 17:25]
  :PROPERTIES:
  :CREATED:  2026-06-07 Sun 15:12
  :ARCHIVE_TIME: 2026-09-06 Sun 22:23
  :ARCHIVE_FILE: ~/dev/orc/PROJECT.org
  :ARCHIVE_CATEGORY: PROJECT
  :ARCHIVE_TODO: CLOSED
  :END:
  :LOGBOOK:
  - State "CLOSED"     from "TODO"       [2026-09-04 Fri 17:25]
  :END:

  Whenever the plugin code receives an output handle from the host, it has to
  verify that the output handle was allocated in the same plugin. Otherwise it
  is unsafe to write into that memory address. If it came from another plugin,
  then this plugin cannot know anything about the datatype or memory layout. To
  check for this, each plugin likely needs to keep a global list of allocated
  decks, and ensure the output handle matches one of them. Access to this global
  list should be thread safe, since the host can call the plugin functions from
  any thread.

  While this is trivial to do in C++ and Rust, it is not trivial to do in a
  cross platform manner in C. For now, I just have a dummy check that always
  returns true (safe). This is good enough for a prototype, but at some point we
  need to figure out a good cross platform thread API for our C SDK.

  Should the host provide the thread API too? How does Parasolid handle it?

  <2026-09-04 Fri 17:25> This task is outdated, based on an old design. This is not relevant
  anymore.

* CLOSED Deferred failures in niche scenarios
  CLOSED: [2026-09-04 Fri 17:26]
  :PROPERTIES:
  :CREATED:  2026-06-07 Sun 07:55
  :ARCHIVE_TIME: 2026-09-06 Sun 22:23
  :ARCHIVE_FILE: ~/dev/orc/PROJECT.org
  :ARCHIVE_CATEGORY: PROJECT
  :ARCHIVE_TODO: CLOSED
  :END:
  :LOGBOOK:
  - State "CLOSED"     from "TODO"       [2026-09-04 Fri 17:26]
  :END:

  If we want to migrate to a nicer system that does more things at runtime
  instead of compile time, in some very niche scenarios, we MAY have to live
  with not knowing type mismatch errors before running some functions. This is
  only the case with types that use some type of polymorphism.

  <2026-09-04 Fri 17:26> This is not interesting at this time.

* DONE tkv3ek - Deck to String Conversion for Display
  CLOSED: [2026-09-05 Sat 13:41]
  :PROPERTIES:
  :CREATED:  2026-09-04 Fri 18:48
  :ARCHIVE_TIME: 2026-09-06 Sun 22:23
  :ARCHIVE_FILE: ~/dev/orc/PROJECT.org
  :ARCHIVE_CATEGORY: PROJECT
  :ARCHIVE_TODO: DONE
  :END:
  :LOGBOOK:
  - State "DONE"       from "NEXT"       [2026-09-05 Sat 13:41]
  - State "NEXT"       from "TODO"       [2026-09-04 Fri 18:53]
  :END:

  This is very useful, and the bar for implementing is so low, that it should be part of the ABI.

  <2026-09-05 Sat 13:15> Got the C side of things working yesterday. Added tests. Implemented the
  Rust side of things today. Adding tests now. Will try to get this merged after that.

  The basic design for string conversion across the FFI boundary is figured out. I could add more
  utilities and helpers, but I prefer not to add those in vacuum. Instead, I should add those when I
  implement apps and features that use the string conversion.

  <2026-09-05 Sat 13:41> Also ended up threading the string conversion through all the Rust host /
  plugin machinery. Added a lot of tests. Ready to merge.

  <2026-09-05 Sat 14:08> Forgot about the Python integration. Doing that now before merging.

  <2026-09-05 Sat 14:34> Python integration done. Test coverage added. Linter warnings fixed.

* CLOSED tkwogf - Overhaul of workflow property serialization
  CLOSED: [2026-09-06 Sun 13:28]
  :PROPERTIES:
  :CREATED:  2026-09-05 Sat 12:41
  :ARCHIVE_TIME: 2026-09-06 Sun 22:23
  :ARCHIVE_FILE: ~/dev/orc/PROJECT.org
  :ARCHIVE_CATEGORY: PROJECT
  :ARCHIVE_TODO: CLOSED
  :END:
  :LOGBOOK:
  - State "CLOSED"     from "DONE"       [2026-09-06 Sun 13:30]
  - State "DONE"       from "NEXT"       [2026-09-06 Sun 13:28]
  - State "NEXT"       from "TODO"       [2026-09-05 Sat 15:22]
  :END:

  The UI app will need to save it's own properties that don't mean anything to any other host app,
  so the property serialization needs to be way more generic. Currently each relevant property is
  serialized explicitly. That is not good enough.

  <2026-09-06 Sun 13:20> I am not sure about this direction. After some more thought, I think may be
  this is not the right way to solve some of these problems. I will drop this for now. I don't need
  to solve the "multiple hosts save their data in the file using the same property system" problem
  right now. I can put that off to when I really need to solve it, or if I come up with the right
  solution for it.

  Instead of overhauling the property system, I can add support for dead end nodes, or transparent
  nodes that just propagate their inputs downstream, that can be used by the UI node editor as
  "inspect" nodes that display the data as either text or geometry. I will do that as part of
  another task.

  Merging the irrelevant edits of this branch into main anyway.

* DONE tkyeb2 - Support 'Inspect' nodes in a workflow
  CLOSED: [2026-09-06 Sun 22:21]
  :PROPERTIES:
  :CREATED:  2026-09-06 Sun 13:28
  :ARCHIVE_TIME: 2026-09-11 Fri 14:10
  :ARCHIVE_FILE: ~/dev/orc/PROJECT.org
  :ARCHIVE_CATEGORY: PROJECT
  :ARCHIVE_TODO: DONE
  :END:
  :LOGBOOK:
  - State "DONE"       from "NEXT"       [2026-09-06 Sun 22:21]
  - State "NEXT"       from "TODO"       [2026-09-06 Sun 13:44]
  :END:

  This can be used by a UI app to display the data in whatever format the user likes. Each host can
  interpret these nodes, or ignore them to it's liking. E.g. a CLI app can use them to log the data.

  <2026-09-06 Sun 22:18> Added an inspect node that doesn't participate in eval. Nothing here that
  can be tested. This will be tested when I write the UI app.

* DONE tl68t6 - Fix the Pyorc build system
  CLOSED: [2026-09-10 Thu 19:23]
  :PROPERTIES:
  :CREATED:  2026-09-10 Thu 19:18
  :ARCHIVE_TIME: 2026-09-11 Fri 14:10
  :ARCHIVE_FILE: ~/dev/orc/PROJECT.org
  :ARCHIVE_CATEGORY: PROJECT
  :ARCHIVE_TODO: DONE
  :END:
  :LOGBOOK:
  - State "DONE"       from "NEXT"       [2026-09-10 Thu 19:23]
  - State "NEXT"       from "TODO"       [2026-09-10 Thu 19:20]
  :END:

  The same linkage cannot be used for a python version agnostic library (which cannot statically
  link against a specific Python), and unit test binaries that need to link statically.

  <2026-09-10 Thu 19:30> Fixed by splitting pyo3's =extension-module= feature out from a plain
  dependency feature into a crate feature of its own, defaulted on. With =extension-module=
  enabled, pyo3 skips linking libpython at build time, since a real Python extension module gets
  those symbols for free from the host interpreter that =dlopen='s it at import time. A =cargo
  test= binary has no such host process, so it needs the symbols linked in, and fails with
  undefined references if =extension-module= is on.

  The fix: define an =extension-module= feature in =pyorc='s own =Cargo.toml= that forwards to
  =pyo3/extension-module=, and default it on. =maturin build= / =maturin develop= build with
  default features, so the wheel is unaffected. Tests are run with =cargo test
  --no-default-features=, which turns the forwarding feature off so pyo3's build script links
  against libpython normally. Also switched =crate-type= to =["cdylib", "rlib"]=, since a pure
  =cdylib= can't be linked as a library dependency, which =cargo test= needs for integration
  tests, doc-tests, etc. Wired =cargo test --no-default-features -p pyorc= into the CI workflow
  for all three platforms so this stays covered going forward.


* DONE tkuzn6 - Node Editor UI App
  CLOSED: [2026-09-10 Thu 21:01]
  :PROPERTIES:
  :CREATED:  2026-08-26 Wed 10:47
  :ARCHIVE_TIME: 2026-09-11 Fri 14:11
  :ARCHIVE_FILE: ~/dev/orc/PROJECT.org
  :ARCHIVE_CATEGORY: PROJECT
  :ARCHIVE_TODO: DONE
  :END:
  :LOGBOOK:
  - State "DONE"       from "NEXT"       [2026-09-10 Thu 21:01]
  - State "NEXT"       from "TODO"       [2026-09-06 Sun 22:31]
  :END:

  To lower the scope of the node editor demo app, I think it should not have a dedicated viewer,
  that stays in sync with the node graph. Instead the node graph should have a set of no-op
  "inspect" nodes, that visualize the data as either text or graphics, potentially with an
  interactive camera. Maybe they support a maximize / minimize option to temporarily fill the
  screen, and explore. Maybe there is a button that pops this view out into a separate window if
  the user wants to keep it around as they move around the canvas. This is still much simpler than
  a general viewer, since it only displays a single output, with no need for concepts such as
  selection.

  <2026-09-04 Fri 18:49> I need a way to display the decks. So I decided to do [[*tkv3ek - Deck to String Conversion for Display][this task]] first.

  <2026-09-05 Sat 15:22> Also need to do [[*tkwogf - Overhaul of workflow property serialization][this task]] first.

  <2026-09-06 Sun 22:33> I added 'Inspect' nodes in a task I just merged. These nodes should be used
  by the node editor UI app to display the data they're connected to. Depending on the label of the
  inspect node, the UI app should either display the tree as text, or display 3d geometry, if the
  data actually represents geometry.

  <2026-09-06 Sun 23:33> Brainstormed some details and created a detailed plan for implementing the
  node editor.

  <2026-09-10 Thu 19:55> Finished implementing it. Only tested it a little bit. Doesn't make sense
  to try and test this too much now. I would rather implement some useful plugin functions, and
  start making some workflows, and this implementation will get tested organically in that
  process. This is good enough for now, to mark this task done after some final review, and clean
  up.

  <2026-09-10 Thu 21:00> Did a final review and a set of improvements.

** DONE Detailed Plan
   CLOSED: [2026-09-10 Thu 19:55]
   :PROPERTIES:
   :CREATED:  2026-09-06 Sun 23:03
   :END:
   :LOGBOOK:
   - State "DONE"       from "NEXT"       [2026-09-10 Thu 19:55]
   - State "NEXT"       from "TODO"       [2026-09-07 Mon 23:39]
   - State "TODO"       from              [2026-09-07 Mon 22:28]
   :END:

*** Custom Rendering (not egui-snarl or egui-graph-edit)

    Existing egui node editor crates (egui-snarl, egui-graph-edit) maintain their own internal graph
    model with their own ID types. Using them would require keeping two parallel graph representations
    in sync -- a constant source of bugs. Instead, render nodes directly from the orc Workflow using
    egui's Painter API:

    - ~Painter::rect_filled~ / ~rect_stroke~ for node bodies (rounded rectangles)
    - ~Painter::circle_filled~ / ~circle_stroke~ for input/output pins
    - ~Painter::text~ for all labels (monospace font throughout the entire UI)
    - ~Painter::add(Shape::CubicBezier { .. })~ for connection curves between pins

    All coordinates are transformed through a pan/zoom affine matrix before drawing.

*** Force-Directed Layout with DAG Flow Constraint

    Nodes are NOT draggable. The layout is computed automatically by a force-directed simulation
    that respects left-to-right DAG flow. Nodes are axis-aligned rectangles with variable sizes
    (inspect nodes are resizable). The layout updates incrementally when topology changes or nodes
    are resized. Positions and sizes are not persisted to disk for now -- they are recomputed from
    scratch when the app opens, and updated in-place during the session.

**** Forces

     - *Bounding-box repulsion:* All node pairs repel each other based on their bounding boxes
       (not point-to-point). Larger inspect nodes push neighbors further away. This prevents
       overlap and maintains readable spacing.
     - *Spring attraction along edges:* Connected nodes (output pin -> input pin) are pulled
       toward each other. Spring rest length is proportional to the sum of the two nodes' widths
       plus a gap.
     - *Vertical centering:* Each node is gently pulled toward the vertical median of its
       connected neighbors, reducing unnecessary vertical spread.

**** DAG Flow Constraint (hard constraint)

     After each simulation step, enforce left-to-right ordering: for every edge (A -> B), ensure
     ~B.x >= A.x + A.width + gap~. If violated, push B rightward. This is applied in topological
     order so that corrections propagate downstream in a single pass.

     Workflow input chips are pinned to x = 0. Workflow output chips are free to settle at whatever
     x the simulation produces (they will naturally end up rightmost since they have no downstream
     nodes pulling them left).

**** Initial Seeding (app startup, no stored positions)

     1. Topological sort gives each node a depth (longest path from any root).
     2. Depth determines initial x position (depth * (typical_node_width + gap)).
     3. Nodes at the same depth are spread evenly along y.
     4. Run full simulation to convergence (iterate until max force magnitude drops below a
        threshold, or a fixed max iteration count is reached).

**** Incremental Updates (topology change, node resize)

     When a node is added, removed, connected, disconnected, or resized:
     1. Keep all existing node positions as-is.
     2. New nodes are seeded near their connected neighbors (or at the center of the viewport if
        disconnected).
     3. Re-run the simulation from current positions for a few iterations until convergence.
     4. Because the simulation is continuous (small perturbation -> small output change), the layout
        shifts smoothly without large jumps.

**** Convergence and Repaint

     The simulation runs as part of the update loop. While forces are above the convergence
     threshold, ~ctx.request_repaint()~ is called each frame to animate the layout settling. Once
     converged, repaints stop (zero CPU). This gives a smooth animation of nodes settling into
     place after a topology change.

*** Hit Testing via Spatial Hash Grid

    Nodes have variable sizes (resizable inspect nodes) and are not in strict layers, so a general
    spatial index is needed. A spatial hash grid provides O(1) average-case point queries.

**** Structure

     A uniform grid where each cell is roughly the size of a typical node (~200x200 canvas units).
     Each node is inserted into all grid cells its bounding box overlaps. A large resized inspect
     node may span several cells -- this is fine.

**** Queries

     To find what is under the cursor:
     1. Transform cursor to canvas coordinates.
     2. Compute grid cell from canvas position.
     3. Test only nodes in that cell (and possibly immediate neighbors for edge cases).
     4. If the cursor is on a node, test its pins linearly (few pins per node).

**** Rebuild

     The grid is rebuilt whenever the layout changes (topology change, resize, or simulation step).
     Rebuild cost is O(n) -- iterate all nodes, insert into cells. This is cheap even for thousands
     of nodes.

*** EditorState

    Uses orc's existing property system (~NodeProperty<T>~, ~OutputProperty<T>~) instead of
    HashMaps. Properties are attached to the workflow's graph and automatically resize when nodes
    are added/removed or garbage collection runs.

    Positions and sizes are transient -- not persisted to disk. They are computed by the layout
    simulation on app startup and updated in-place during the session. Persistence of UI state is
    a future concern.

    #+begin_src rust
    struct EditorState {
        workflow: Workflow,
        // Properties (attached to workflow graph, auto-resize with topology)
        node_positions: NodeProperty<[f32; 2]>,
        node_sizes: NodeProperty<[f32; 2]>,              // for resizable inspect nodes
        computed_outputs: OutputProperty<Option<OrcHandle>>,   // cached execution results
        nested_exec_state: NodeProperty<Option<NestedExecState>>,  // per-call internal state
        // Spatial index (rebuilt on layout change)
        spatial_grid: SpatialHashGrid,
        // Layout simulation
        layout_converged: bool,                        // false while simulation is settling
        // UI transients (not persisted)
        pan_offset: Vec2,
        zoom: f32,
        selected_nodes: HashSet<usize>,               // NH indices
        connecting: Option<ConnectState>,              // active wire drag from a pin
    }
    #+end_src

*** Incremental Execution

    <2026-09-09 Wed> Brainstormed the execution engine and threading model in detail. Nesting is
    out of scope for now -- see Phase 5, which is where ~NestedExecState~ (below) actually
    belongs; this section only covers a flat, non-nested workflow. Dagger gets its own executor,
    separate from ~Workflow::run~ -- that function is a live public API other hosts depend on
    (pyorc's ~run_workflow~, ~kbb_cli_host~'s tests both call it with its current
    signature/behavior), so it stays as-is rather than growing incremental/threading concerns
    bolted on.

**** Why every node is a root

     ~Workflow::run~'s traversal starts only from ~self.workflow_outputs~ (the explicit named
     terminal outputs set via ~set_outputs~). Dagger never calls ~set_outputs~/~set_inputs~, so
     that list is always empty here, and Inspect nodes -- dead ends, no outputs -- would never be
     reached by that traversal at all. Dagger's own executor instead walks every node
     (~workflow.node_iter()~ as roots, same shape as ~layout::compute_depths~), so Inspect dead
     ends and anything else get evaluated regardless of whether they feed a declared output.
     Nodes flagged as part of a cycle are skipped -- but the executor does its own cycle check
     each time it walks, rather than trusting ~state.node_in_cycle~, which ~layout::compute_layout~
     only ever computes once (the first ~measure~ call) and does not update after later
     topology edits.

**** Staleness tracking

     - ~computed_outputs: OutputProperty<Arc<OrcHandle>>~ in ~EditorState~, cached per output
       pin (~Arc~-wrapped -- see "Handle lifetime across threads" below). The "not yet computed"
       sentinel is ~free_fn.is_none()~ on the wrapped handle (the zeroed default) -- *not*
       equality against a zeroed ~handle~ id, since ~HANDLE_COUNTER~ starts at 0 and a
       legitimately computed value could be assigned handle id 0. Only ~free_fn~ tells "no
       backing deck" apart from "really is handle 0", matching the pattern already used
       elsewhere in the crate (e.g. the constant-deletion regression test in ~interaction.rs~).
     - ~dirty_version: NodeProperty<u64>~ per node, bumped (not just set to a bool) on a direct
       edit -- connect, disconnect, add, delete, a constant's value changing -- and propagated
       forward to every downstream node. This requires a small addition to ~orc_sdk~: a public
       forward-adjacency accessor on ~Workflow~ (e.g. ~output_links(OH)~), thinly wrapping the
       ~Graph::output_links~ machinery that already exists internally (an intrusive linked list
       per output, walked in O(fan-out)) but isn't reachable from outside the crate today --
       ~Workflow~ only exposes the backward lookup (~input_source~).
     - Dispatch readiness is tracked via a small worklist of "candidate" nodes -- anything just
       touched by a staleness-propagation pass, or whose upstream just finished computing -- each
       checked locally against its own (small) fan-in when popped. This is deliberately not a
       fully precise per-edge in-degree counter (more bookkeeping than it is worth at typical
       node arity) and not a per-frame full-graph scan either -- work stays proportional to
       whatever actually changed, not to graph size.

**** Thread pool

     A fixed-size pool of ~max(available_parallelism() - 1, 1)~ long-lived worker threads,
     spawned once and blocking on a shared ~Arc<Mutex<Receiver<NodeJob>>>~ -- no thread creation
     or teardown during interactive use, and independent branches genuinely run in parallel
     (whichever worker is free picks up the next ready job), which is the whole reason for a pool
     over a single background thread. A single-background-thread design was considered and
     rejected: it's simpler and sidesteps the handle-lifetime issue below, but gives up true
     cross-branch parallelism, which is a hard requirement here, not a nice-to-have.

     Results come back over a second ~mpsc~ channel, drained via ~try_recv()~ on the UI thread
     once per frame -- the UI thread never blocks on execution, matching or exceeding the
     original 100ms-poll idea.

     ~NodeJob~ carries everything a worker needs without ever touching the live ~Workflow~ (whose
     ~Rc~/~RefCell~-based properties are not ~Send~): the node handle, the plugin function
     pointer, its inputs as cloned ~Arc<OrcHandle>~ (see "Handle lifetime across threads" below),
     the output count, a cancellation ~ctx~ id, and the ~dirty_version~ it was launched for.
     ~NodeResult~ carries the node handle, that same ~ctx~/launched version, and the outcome:
     ~Ok(outputs)~, ~Err(OperationCancelled)~, or ~Err(other)~.

**** Cancellation is per node job, not per pass or global

     One ~ContextArena<bool>~ slot per dispatched job (mirrors the existing
     ~SERIAL_CONTEXT_ARENA~ pattern already used for ~serial_write_callback~). The plugin's
     ~check_cancellation(ctx)~ callback reads it; whether a plugin checks it at all is entirely
     up to whoever implemented that node -- the host impose no policy here. Only the specific job
     whose own node gets invalidated has its flag flipped: editing an unrelated, independent
     branch must never disturb a node that is currently running, since its output is still
     perfectly valid. This relies on ~ORC_ERROR_OPERATION_CANCELLED~ (added in commit e1b1320) to
     tell a cooperative abort apart from a real failure returned by the same call.

**** Handle lifetime across threads: ~Arc<OrcHandle>~

     ~OrcHandle~ already has ~impl Drop { fn drop(&mut self) { self.free(); } }~ (~ffi.rs:212~) --
     dropping an owning handle calls ~.free()~, dropping a borrowed one (~free_fn: None~, as
     produced by ~.borrowed()~) is already a safe no-op. It's a move-only, RAII-owning type
     already (why ~Workflow::run~ has to ~std::mem::take~ out of ~computed_outputs[o.idx]~
     instead of just reading it). So sharing one across an in-flight job needs nothing more than
     the standard tool for shared ownership: ~computed_outputs: OutputProperty<Arc<OrcHandle>>~.

     Dispatching a job clones the ~Arc~ for each borrowed input and stores the clones directly in
     ~NodeJob~. On the worker, right before the actual plugin call, an ~OrcHandleBorrowed<'_>~ is
     derived locally from each ~Arc<OrcHandle>~ (~arc.borrowed()~) into a short-lived stack
     buffer -- an ordinary, fully compiler-checked borrow, since the owning ~Arc~ clones are
     sitting on the same stack frame the whole time. ~OrcHandleBorrowed~ never has to cross the
     channel or have its lifetime unsafely asserted away; only the ~Arc~ travels, and it's
     ordinarily ~Send~.

     Committing a fresh result is just ~computed_outputs[oh] = Arc::new(new_handle)~. If the UI
     thread held the only reference, the old ~Arc~ drops immediately and ~.free()~ runs right
     there, via ~OrcHandle~'s own ~Drop~ -- nothing in dagger calls ~.free()~ explicitly anywhere.
     If some worker still holds a clone (an in-flight downstream job that captured the old
     value), the old ~Arc~ simply lives on until that worker drops its copy, whereupon ~.free()~
     runs automatically -- on whichever thread happens to drop last, which is why a plugin's
     ~free_fn~ must be safe to call from any thread (the same expectation the whole design
     already places on plugin code called concurrently in general). No reader-count bookkeeping,
     no pending-commit staging slot, no ~orc_sdk~ changes at all -- this works uniformly for
     built-in and plugin-owned handles alike, since it never needs to look behind the handle at
     what's managing the actual backing memory (which, for a plugin-owned type, may not even be
     Rust).

     Relies on one existing invariant: the executor always allocates a *fresh* handle id per
     computed output (never reuses one), matching how ~Workflow::run~ already works via
     ~handle_counter.fetch_add~. ~DeckRegistry::alloc_with_value~ has a same-id-reuse path
     (~util.rs:134~) that mutates a built-in deck's contents in place under a write lock when the
     id is already occupied with a matching type -- irrelevant as long as the executor never
     reuses an id, but worth remembering if that ever changes.

**** Commit policy on job completion

     - ~Ok(outputs)~ is always committed immediately (replacing the ~Arc<OrcHandle>~ in
       ~computed_outputs~ -- no staging needed), even if the node was invalidated again after
       this job launched -- a stale-but-real value beats showing
       nothing, and this is exactly what makes continuously dragging a slider feel alive while a
       slow, non-cooperative node is mid-flight. The node is still redispatched afterward if it's
       still marked dirty.
     - ~Err(OperationCancelled)~ commits nothing; the node is simply eligible for redispatch again
       right away.
     - Any other ~Err~ is a genuine fault: shown on the node, and the node settles rather than
       retrying every time the scheduler looks for ready work -- it only becomes a candidate
       again once a future edit actually touches it or its inputs.

**** In-progress indicator

     A node with an in-flight job gets a looping left-to-right pulse/sweep drawn into its title
     bar fill (indefinite-progress-bar style), driven by wall-clock time. Continuous repaint is
     requested every frame while any node is in-flight -- gated on "something is actually
     running", so it costs nothing the rest of the time, but needed for the sweep to animate
     smoothly (a 100ms poll interval, as originally planned, would make it stutter).

*** Nested Workflow Execution & Editing (superseded sketch)

    <2026-09-09 Wed> First pass at this (recursive ~NestedExecState~, cloning the nested workflow
    on open, ~show_viewport_deferred~, a 3-way close prompt) turned out more complicated than it
    needed to be.

    <2026-09-10 Thu> Simplified: no cloning at all. Opening a NestedCall node *moves* the actual
    ~Workflow~ out of ~nested_workflows[name]~ into a new ~EditorState~ pushed onto a stack, so
    the pop-out edits the real object in place; closing moves it back. See "Phase 5: Nested
    Workflow Editing" under Implementation Phases for the current, authoritative design and
    checklist -- this section is kept only as a pointer, the detail now lives there.

*** Workflow Inputs/Outputs Display

    - Workflow inputs are rendered as minimal "chip" pill-shaped nodes at layer 0 (the leftmost
      layer). They have only an output pin and a label showing the input name. They are roots -- no
      input pins. Distinct styling (different color/outline) to visually distinguish from function
      nodes.

    - Workflow outputs are rendered as chip nodes at the last layer (rightmost). They have only an
      input pin and a label showing the output name. They are dead ends -- no output pins. Same
      distinct styling as input chips.

    - NestedCall nodes are displayed as regular function nodes with the nested workflow name as the
      title bar text. Double-click to open the nested editor.

*** Right-Click Context Menu

    The menu opens on a plain right-click, without shift -- shift + right-drag is the pan
    gesture, so the two share the button and are told apart by the modifier.

    Two trigger points with slightly different behavior:

**** Right-click on empty canvas

     Adds a disconnected node at the click position.

**** Right-click on an output pin

     Adds a node AND connects its first input to the clicked output pin. The node is placed at the
     position determined by the layout recomputation after the connection is made.

**** Menu UI (same for both triggers)

     1. A popup appears. The top element is a *text field that is immediately focused* so the user
        can start typing without clicking.
     2. Below the text field is a list of options.
     3. *If the user types into the text field:* the option list is replaced by search results
        filtered from all available functions in the loaded PluginSet. The initial filtering
        implementation is ~name.starts_with(query)~. The filtering logic is behind a trait so it
        can be replaced with levenshtein distance, fuzzy matching, or other strategies later:
        #+begin_src rust
        trait FunctionFilter {
            fn filter(&self, query: &str, functions: &[FuncInfo]) -> Vec<usize>; // indices into functions
        }
        #+end_src
     4. *If the user does NOT type and instead uses arrow keys:* they navigate the default options:
        - "Add Constant Node"
        - "Add Inspect Node"
        - "Session Info..." (opens a separate window showing loaded plugins, their functions, and
          their types -- the information that would otherwise go in a sidebar palette)
     5. Selecting a function from the search results or an option from the default list creates the
        appropriate node. If the menu was triggered from an output pin, the new node's first input
        is also connected to that pin.
     6. After node creation, the layout recomputes.

*** Inspect Node Display

    Inspect nodes are dead-end visualization nodes with 1 input, 0 outputs, and a label string.
    The label determines the display mode.

    - *Text display:* Render the ~deck_to_str()~ output of the connected upstream data in a
      scrollable monospace text area embedded in the node body.
    - *Resizable:* Inspect nodes can be resized by the user (stored in ~node_sizes~ property).
    - *Maximize/minimize:* A toggle to expand the inspect node to fill the viewport, or shrink it
      back to its normal size.
    - *Pop-out:* A button to pop the inspect view into a separate OS window via
      ~ctx.show_viewport_deferred()~. The user can keep this window open as they navigate the
      canvas.
    - *Future 2D/3D viewports:* When the label indicates geometry data, the inspect node will
      embed a wgpu render pass via egui's ~PaintCallback~ and support pan/zoom/orbit within that
      embedded viewport. (Out of scope for initial implementation.)

*** Repaint Strategy

    Reactive mode -- zero CPU when fully idle. ~ctx.request_repaint()~ is called only when needed:

    - Canvas interaction: pan, zoom, box select
    - Pin connection wire drag (drawing the in-progress Bezier to the cursor)
    - Inspect node interaction: scrolling text content, resizing the inspect viewport, future
      pan/zoom/orbit within an embedded 2D/3D viewport
    - Layout settling: stale -- Phase 3b removed the force-directed simulation entirely, so this
      no longer applies. Layout is now a one-shot computation (see "Force-Directed Layout with
      DAG Flow Constraint" above, itself written before that removal).
    - Execution: continuous repaint every frame while any node has an in-flight job (both to
      drain the results channel and to animate the in-progress sweep on that node's title bar --
      see "In-progress indicator" under "Incremental Execution"). Replaces the original
      100ms-poll idea, which would make the sweep stutter. Stops the instant nothing is running.

    When no interaction is active, no layout is settling, and no execution is running, the app
    sleeps completely and consumes zero CPU.

*** main.rs: Host Setup

    The host setup (OrcHost, PluginSet loading, DeckRegistry, handle counter, host callbacks) lives
    directly in ~main.rs~, following the same pattern as ~rust/kbb_cli_host/src/main.rs~. No
    separate host.rs file.

    Key elements to replicate from kbb_cli_host:
    - ~static REGISTRY: LazyLock<DeckRegistry>~
    - ~static HANDLE_COUNTER: AtomicU64~
    - ~static SERIAL_CONTEXT_ARENA: LazyLock<ContextArena<Vec<u8>>>~
    - ~static PLUGIN_SET: LazyLock<PluginSet>~
    - Host callbacks: ~host_alloc~, ~host_dealloc~, ~serial_write_callback~,
      ~host_create_proxy_deck~, ~report_message~
    - ~HOST: OrcHost~ constant with the callback table
    - ~host_clone_orc_handle~ helper

*** Implementation Phases

**** DONE Phase 1: Host Setup + Render + Layout
     CLOSED: [2026-09-08 Tue 15:34]
     :LOGBOOK:
     - State "DONE"       from "NEXT"       [2026-09-08 Tue 15:34]
     - State "NEXT"       from "TODO"       [2026-09-07 Mon 23:12]
     - State "TODO"       from              [2026-09-07 Mon 23:11]
     :END:

     - [X] Host setup in main.rs: replicate kbb_cli_host pattern (OrcHost, PluginSet, DeckRegistry,
       handle counter, host callbacks). Load plugins at startup.
     - [X] EditorState struct with Workflow, NodeProperty for positions/sizes, layout state.
     - [X] Create a test workflow via a Python script (using pyorc API), save it to
       ~workflows/test.orc~ in the project root. Load that file in dagger at startup.
     - [X] Node rendering: rounded rect body (color by NodeInfo variant), monospace title bar,
       input pins (circles on left edge with labels), output pins (circles on right edge with
       labels). Workflow input/output chips as pill-shaped nodes with distinct styling.
     - [X] Link rendering: cubic Bezier curves from output pin center to input pin center, control
       points extending horizontally.
     - [X] Force-directed layout: bounding-box repulsion, spring attraction along edges, vertical
       centering, DAG flow constraint (hard). Topological seeding on startup, convergence
       animation.
     - [X] Pan/zoom canvas: shift + right-drag to pan, scroll wheel to zoom about the cursor
       (clamped 0.1..5.0). Screen-to-canvas coordinate transform.

       <2026-09-08 Tue 15:34> Pan was originally specified as middle-drag. Changed to shift +
       right-drag to match Grasshopper. Requiring shift leaves a plain right-click free for the
       context menu, so the two gestures can share the button.

**** DONE Phase 1B: Misc clean up after phase 1
     CLOSED: [2026-09-08 Tue 17:08]
     :PROPERTIES:
     :CREATED:  2026-09-08 Tue 15:44
     :END:
     :LOGBOOK:
     - State "DONE"       from "NEXT"       [2026-09-08 Tue 17:08]
     - State "NEXT"       from "TODO"       [2026-09-08 Tue 16:51]
     :END:

     - [X] The app should respect the argument names provided by the plugin via the ABI. Just added
       that stuff to the SDK. Check it and use it in the rendering. The rendered blocks should show
       the names, and should be appropriately sized based on the largest input / output names. This
       also addresses your concerns about pin labels.
     - [X] ~push_link~ is not responsible for checking for cycles. The graph is allowed to exist
       with cycles. It should be shown as an error state. For now, just make the nodes in a cycle
       red to indicate a problem. [[file:rust/orc_sdk/src/dag/workflow.rs::pub fn run(][This function]] shows how to detect cycles when walking the
       graph. Use that approach when computing the layout too. But you still need to seed the layout
       with something, just stop the traversal and use an approximate position based on what you
       know at that point, and render the block in an error state.

**** DONE Phase 2: Hit Testing + Interaction + Context Menu
     CLOSED: [2026-09-08 Tue 19:00]
     :PROPERTIES:
     :CREATED:  2026-09-08 Tue 18:21
     :END:
     :LOGBOOK:
     - State "DONE"       from "NEXT"       [2026-09-08 Tue 19:00]
     - State "NEXT"       from "TODO"       [2026-09-08 Tue 17:51]
     - State "TODO"       from              [2026-09-07 Mon 23:11]
     :END:

     *Hit testing:*
     - [X] No spatial index. Each node's body and each pin get their own
       ~ui.interact(rect, id, Sense::click_and_drag())~ call every frame, in
       ~interaction::update~. egui's own hit test is itself a linear scan and already resolves
       overlaps by paint order (pins are interacted after the body, so they win ties over the
       sliver of themselves that overlaps the node's edge), so this scales the same way and
       needs no index to rebuild every time the layout moves. Matches Qt's own guidance
       (~QGraphicsScene::NoIndex~ for small, frequently-animated scenes) and how existing egui
       node-graph crates (egui_snarl, egui_node_graph) do it.
     - [X] Point query: no separate function. The ~Response~ returned by each node/pin's
       ~ui.interact~ answers hover/click/drag directly (~.dragged()~, ~.clicked()~,
       ~.drag_started()~, ...), keyed by stable ~Id~s derived from ~NH~/~IH~/~OH~.
     - [X] No ~HitTarget~ enum. Each node exposes two kinds of hit target instead: its pins
       (drag to start or rewire a connection) and the rest of its body (drag to move the node).
       Pin positions are still computed from the owning node's position + fixed offsets per pin
       index (~render::input_pin_pos~/~output_pin_pos~).

     *Dragging and Connecting pins:*

     If the dragging starts on the title of a node, it should drag the node along, but the force
     directed graph should still try to revert to it's equilibrium. If the drag starts at a pin, it
     should create a link.

     - [X] Implement dragging for nodes. This is meant to just make the graph rendering feel
       alive. Not meant to actually move nodes, because the force-directed-graphs will restore the
       nodes to their equilibrium, position, which may not be the same as their position before
       dragging, same as obsidian graph rendering.

     - [X] Click output pin -> enter connecting state, draw in-progress Bezier from pin to cursor.
     - [X] Release on input pin -> call ~workflow.connect()~, trigger layout recompute.
     - [X] Release on empty canvas or invalid target -> cancel, no connection made.
     - [X] Dragging from an already-connected input pin disconnects it first, then enters connecting
       state from the upstream output (rewire gesture).

     *Selection:*
     - [X] Click node -> select it, deselect all others.
     - [X] Shift+click node -> toggle that node's selection without affecting others.
     - [X] Click empty canvas -> deselect all.
     - [X] Click+drag on empty canvas -> draw a selection rectangle, select all nodes whose bounding
       boxes intersect it.

     *Delete:*
     - [X] Delete key removes all selected nodes (~workflow.delete_node()~ for each).
     - [X] Triggers layout recompute and staleness propagation for incremental execution.

     *Right-click context menu:*
     - [X] Right-click (no shift) on empty canvas -> popup at click position, creates
       disconnected node.
     - [X] Right-click (no shift) on output pin -> popup, creates node AND connects its first
       input to that pin.
     - [X] Popup UI: focused text field at top. Typing filters available functions via FunctionFilter
       trait (~starts_with~ initially, swappable for fuzzy/levenshtein later). Arrow keys without
       typing navigate default options: "Add Constant Node", "Add Inspect Node",
       "Session Info...".
     - [X] Selecting an entry creates the node, closes the popup, triggers layout recompute.

     *Session info window:*
     - [X] Opened from context menu "Session Info..." option.
     - [X] Separate ~egui::Window~ showing loaded plugins, their functions (name, n_inputs, n_outputs,
       description), and their registered types.

     <2026-09-08 Tue 18:20> Tweaks to the UX after testing:

     - [X] When I am about to release a cable connection (drag) at an input pin, I get no visual
       feedback. I would like the emtpy circle of the receiving pin to thicken up (without fill). Do
       this in immediate mode style.
     - [X] In the right click menu, I want the entire row to actively select that entry, and not
       just the bounds of the text. Smaller targets are harder to hit with the mouse.
     - [X] Nuanced selection rectangles. Dragging to the right should create a selection rectangle
       that only selects nodes that are fully inside it. This rectangle should have a solid stroke
       line. Dragging to the left creates a selection rectangle that selects all nodes that overlap
       with it. This selection rectangle should have a dashed line stroke if egui supports this
       efficiently, otherwise leave it as solid too.
     - [X] Make the fill colors of both selection rectangles more transparent, maybe half as opaque
       as they are now.
     - [X] Inspect nodes should have the title "inspect" instead of "a", as it is now.
     - [X] Use case insensitive substring check to filter node names.

     <2026-09-08 Tue 18:53> Some more bug fixing and testing, almost done.

**** DONE Phase 3: File I/O
     CLOSED: [2026-09-08 Tue 19:26]
     :LOGBOOK:
     - State "DONE"       from "NEXT"       [2026-09-08 Tue 19:26]
     - State "NEXT"       from "TODO"       [2026-09-08 Tue 19:14]
     - State "TODO"       from              [2026-09-07 Mon 23:11]
     :END:

     - [X] File menu in the top bar: Save, Save As, Open.
     - [X] Save: serialize workflow via ~workflow.write_to_msgpack()~, write to file path.
     - [X] Open: read file, deserialize via ~Workflow::read_from_msgpack()~, build new EditorState,
       run layout from scratch (topological seeding + convergence).
     - [X] Track current file path for subsequent saves. Prompt for path on first save (Save As).
     - [X] Dirty flag: track whether workflow has been modified since last save. Show indicator in
       title bar (e.g. asterisk).

     <2026-09-08 Tue 19:18> Some tweaks after testing:

     - [X] Enable use of keyboard shortcuts: Ctrl-n to open a new empty workflow, Ctrl-o to open
       one, Ctrl-s to save, and Ctrl-Shift-s to save-as. Also before throwing away unsaved data,
       (when opening another file or a new workflow), the user should be prompted to optionally save
       their work. This is the same as any other app on earth.
     - [X] Show these shortcuts next to the menu options, with righ alignment. Again very
       conventional UI.

**** CLOSED Phase 3b: Try out Barnes-Hut method to speed up the force directed graph.
     CLOSED: [2026-09-09 Wed 10:19]
     :PROPERTIES:
     :CREATED:  2026-09-08 Tue 23:33
     :END:
     :LOGBOOK:
     - State "CLOSED"     from "NEXT"       [2026-09-09 Wed 10:19]
     :END:

     The performance of the force directed graph updates is O(n^2), with this method we can bring it
     down to O(n * log(n)).

     <2026-09-09 Wed 00:07> Large notebooks display strange behavior. I think the seed positions
     computed before the force directed graph takes over, are very bad.

     <2026-09-09 Wed 00:25> Force directed graph was a wrong move. Getting rid of it
     altogether. More important things need attention right now.

**** DONE Phase 4: Execution + Inspect Display
     CLOSED: [2026-09-09 Wed 21:39]
     :PROPERTIES:
     :CREATED:  2026-09-07 Mon 23:11
     :END:
     :LOGBOOK:
     - State "DONE"       from "NEXT"       [2026-09-09 Wed 21:39]
     - State "NEXT"       from "TODO"       [2026-09-08 Tue 19:31]
     - State "TODO"       from              [2026-09-07 Mon 23:11]
     :END:

     <2026-09-09 Wed 15:55> Added a new error code for cancellation.

     <2026-09-09 Wed> Brainstormed the execution engine and threading model to a concrete design
     (see "Incremental Execution" under the Detailed Plan above for the full rationale). Nesting
     is out of scope for this phase — moved to Phase 5. Dagger gets its own executor; the
     existing ~Workflow::run~ is left untouched, since pyorc and ~kbb_cli_host~ already depend on
     its current signature and behavior.

     *orc_sdk prerequisite:*
     - [X] Add a public forward-adjacency accessor on ~Workflow~ (e.g. ~output_links(OH)~),
       wrapping the ~Graph::output_links~ machinery that already exists internally but isn't
       reachable from outside the crate. Needed for downstream staleness propagation and
       worklist scheduling without full-graph scans. (No other ~orc_sdk~ changes needed —
       handle-lifetime safety below is built entirely out of types that already exist.)

     *Staleness tracking:*
     - [X] ~computed_outputs: OutputProperty<Arc<OrcHandle>>~ in EditorState, cached per output
       pin — see "Handle lifetime across threads" below for why it's ~Arc~-wrapped.
       "Not yet computed" sentinel is ~free_fn.is_none()~, not equality on the raw ~handle~ id
       (id 0 is a legitimate value once ~HANDLE_COUNTER~ has issued it).
     - [X] ~dirty_version: NodeProperty<u64>~, bumped (not just set true) on a direct edit and
       propagated forward to every downstream node via the new adjacency accessor.
     - [X] Dispatch readiness via a small worklist of touched/just-resolved candidate nodes, each
       checked against its own (small) fan-in when popped — not a full per-frame graph scan, and
       not a fully precise per-edge in-degree counter either.

     *Own executor (not ~Workflow::run~):*
     - [X] Walks every node (~workflow.node_iter()~ as roots), not just declared
       ~workflow_outputs~ (which dagger never sets), so Inspect node dead ends are reached.
     - [X] Skips nodes whose ~dirty_version~ already matches what their cached output was
       computed for; only stale nodes are dispatched.
     - [X] Nodes flagged ~in_cycle~ are skipped, via the executor's own cycle check each walk —
       layout's cached ~node_in_cycle~ is only computed once (first ~measure~) and goes stale
       after later topology edits.
     - [X] ~NodeInfo::NestedCall~ is left unimplemented in this executor (no UI path creates one
       yet); revisit alongside Phase 5.

     *Thread pool:*
     - [X] Fixed-size pool, ~max(available_parallelism() - 1, 1)~ long-lived worker threads,
       spawned once, blocking on a shared ~Arc<Mutex<Receiver<NodeJob>>>~ — no thread
       creation/teardown during interactive use, independent branches run genuinely in parallel.
     - [X] Results delivered over a second ~mpsc~ channel, drained via ~try_recv()~ on the UI
       thread once per frame — the UI thread never blocks.
     - [X] ~NodeJob~: node handle, plugin function pointer, inputs as cloned ~Arc<OrcHandle>~
       (an ~OrcHandleBorrowed<'_>~ is derived from each, locally on the worker, right before the
       actual call — same zero-copy pattern as ~Workflow::run~'s ~.borrowed()~, just constructed
       just-in-time instead of shipped across the channel), output count, a cancellation ~ctx~
       id, and the ~dirty_version~ it was launched for.
     - [X] ~NodeResult~: node handle, that same ~ctx~/launched version, and the outcome —
       ~Ok(outputs)~, ~Err(OperationCancelled)~, or ~Err(other)~.

     *Cancellation — per node job, not per pass or global:*
     - [X] One ~ContextArena<bool>~ slot per dispatched job (mirrors ~SERIAL_CONTEXT_ARENA~).
       ~check_cancellation(ctx)~ reads it; whether a plugin checks it at all is entirely up to
       whoever implemented that node.
     - [X] Only the specific job whose own node gets invalidated has its flag flipped — editing
       an unrelated, independent branch must never disturb a node that's currently running.
     - [X] Relies on ~ORC_ERROR_OPERATION_CANCELLED~ (added in commit e1b1320) to tell a
       cooperative abort apart from a real failure.

     *Handle lifetime across threads (~Arc<OrcHandle>~, no wrapper type):*
     - [X] Don't reach into ~DeckRegistry~'s own internal ~Arc~ (~util.rs:96~) — it only exists
       for ~TypeOwner::BuiltIn~ handles; a plugin-owned handle's storage lives entirely on the
       other side of the FFI boundary (possibly not even Rust), with no such ~Arc~ to acquire.
     - [X] No guard/wrapper type needed either — ~OrcHandle~ already has ~impl Drop { fn
       drop(&mut self) { self.free(); } }~ (~ffi.rs:212~), so plain ~Arc<OrcHandle>~ is enough:
       cloning it for each borrowed input and holding the clone in the ~NodeJob~ keeps the
       handle alive exactly as long as the job needs it, uniformly regardless of whether the
       handle is built-in or plugin-owned.
     - [X] Committing a result is just ~computed_outputs[oh] = Arc::new(new_handle)~ — the old
       ~Arc~ drops (and, via ~OrcHandle~'s own ~Drop~, frees) immediately if nothing else holds
       a clone, or lingers until whichever in-flight job does drop its clone. No explicit
       ~.free()~ call anywhere in dagger's own code, no reader-count bookkeeping, no
       pending-commit staging slot.
     - [X] Relies on the executor always allocating a fresh handle id per computed output (never
       reusing one), matching ~Workflow::run~'s existing convention.

     *Commit policy on job completion:*
     - [X] ~Ok(outputs)~ is always committed immediately (~computed_outputs[oh] = Arc::new(...)~,
       no staging needed), even if the node was invalidated again after this job launched. The
       node is still redispatched afterward if still marked dirty.
     - [X] ~Err(OperationCancelled)~ commits nothing; the node is simply eligible for redispatch
       right away.
     - [X] Any other ~Err~ is a genuine fault: shown on the node, and the node settles (does not
       retry every time the scheduler looks for ready work) until a future edit touches it again.

     *In-progress UI indicator:*
     - [X] A node with an in-flight job gets a looping left-to-right pulse/sweep drawn into its
       title bar fill (indefinite-progress-bar style), driven by wall-clock time.
     - [X] Continuous repaint requested every frame while any node is in-flight (replaces the
       original 100ms-poll idea, which would make the sweep stutter).

     *Inspect node display:*
     - [X] Render ~deck_to_str()~ output of connected upstream data in a scrollable monospace text
       area embedded in the node body. ~DeckView~'s recursive ~child()~/~advance_iter()~ (already
       exercised by ~kbb_cli_host~'s ~to_str_groups~ test helper) is the reference for turning
       the resulting ~Deck<u8>~ handle into readable strings. This needs to be done in a particular
       way. The way deck<u8> is displayed should resemble how decks are displayed now. It resembles
       an ascii ruler structure indicated with box drawing characters of hyphens. Except we don't
       want to render individual characters along this ruler. Instead we should collapse it by one
       level, and render first level (list) as a single string item. Ruler structure still holds for
       lists of strings, and lists of lists of strings etc.

       Here's an example. This is the original deck of numbers:

       Original deck:

         2 ─────┤ 10
                ┤ 20
            1 ──┤ 30

       If I display the string deck (Deck<u8>) directly, it will look like:

         3 ────────┤ 49
                   ┤ 48
               1 ──┤ 50
                   ┤ 48
            2 ─────┤ 51
                   ┤ 48

       Displaying in this form is not useful. The second deck should instead be interpreted and
       displayed in the form:

           2 ─────┤ 10
                  ┤ 20
              1 ──┤ 30

       Just so happens it looks identical to the orignal deck because these are integers, but for
       complex numbers, it will make them readable. This is because the ascii letters 48 and 49
       represent the first value "10".

     - [X] Inspect nodes are resizable by the user (drag bottom-right corner), size stored in
       ~node_sizes~. No layout simulation reacts to the new size anymore (Phase 3b removed it) —
       resizing just grows the node in place; neighbors don't move.
     - [X] Pop-out button: open inspect view in a separate OS window via
       ~ctx.show_viewport_deferred()~.
     - [X] In all states, the inspect window displays text, and will have scroll bars in horizontal
       and vertical directions as needed.

     <2026-09-09 Wed 21:25> Inspect node display is decent. Can iterate and refine later as I use
     the app more.

     *Const node display:*
     - [X] The display story for most const nodes and inpsect nodes is the same. The only difference is
       that const owns the data that is displayed, whereas inspect nodes display data they receive
       from upstream, data that they do not own.
     - [X] The second difference is editability. Since the const nodes own the data, and they're the
       'generators' of the DAG, we can make them editable. This will require making them text boxes,
       that display the values. Structure cannot be edited, at least not right now. For now, I want
       each value in the deck, displayed with this ruler structure, to be editable like a text
       box. That means the UI element should parse those strings into either f64 or i64 deck
       depending on the type of data the constant node holds.

     <2026-09-09 Wed 21:25> First pass at editable constants is decent. Need to refine the UI a
     little bit before marking this phase as done.

**** DONE Phase 5: Nested Workflow Editing
     CLOSED: [2026-09-10 Thu]
     :PROPERTIES:
     :CREATED:  2026-09-09 Wed 22:09
     :END:
     :LOGBOOK:
     - State "DONE"       from "NEXT"       [2026-09-10 Thu]
     - State "NEXT"       from "TODO"       [2026-09-09 Wed 21:43]
     - State "TODO"       from              [2026-09-07 Mon 23:11]
     :END:

     <2026-09-10 Thu> Revised after more thought -- dropped cloning entirely in favor of moving
     the real ~Workflow~ in and out of a stack of editors. See the rationale inline below.

     <2026-09-10 Thu> Implemented. One deliberate scope-down from the plan below: background
     execution of a NestedCall node runs the whole nested workflow synchronously, on the UI
     thread, via the existing (already correct, already tested) ~Workflow::run~ -- not the
     compound-key incremental/parallel scheduler this section originally sketched. ~Workflow~
     can never be ~Send~, so a nested workflow's own nodes can't be shipped onto the background
     pool the way a plain Function's can; doing that properly means teaching this scheduler to
     walk a second ~Workflow~'s handle space with its own per-call-instance staleness tracking,
     which is a substantially bigger undertaking than the rest of this phase combined. What's
     implemented is fully correct -- verified end to end against a real generated fixture calling
     a nested workflow, see ~nested.rs~'s ignored ~t_real_fixture_opens_edits_and_recomputes_end_to_end~
     -- but a slow nested workflow blocks the UI for its own duration and gets no incremental
     caching between runs, unlike every other node kind. Left as a follow-up if it ever matters in
     practice (~exec::dispatch_nested_call~ has the full rationale).

     *Background execution of NestedCall nodes:*
     - [X] ~Workflow~ stays single-threaded/UI-thread-only, exactly as today -- no SDK-level
       thread-safety changes. A ~Workflow~ can never be ~Send~ (it embeds ~Property<H, T>~, which
       is ~Rc<RefCell<...>>~-backed), and that's fine: it never needs to cross a thread boundary.
     - [X] A NestedCall node dispatches by taking real ownership of its nested ~Workflow~ (via
       ~take_nested_workflow~/~put_nested_workflow~, new orc_sdk accessors) and running it
       synchronously to completion with ~Workflow::run~ -- see the scope-down note above; this
       replaces the compound-key scheduler generalization originally planned here.
     - [X] A NestedCall's own input values are gathered the same way a Function node's inputs
       already are (constant -> ~host_clone_orc_handle~, upstream output -> existing ~Arc~ clone),
       then handed to the nested workflow's dangling workflow-input pins directly as
       ~Workflow::run~'s own ~inputs~ slice -- never by wiring or writing anything into the shared,
       canonical ~nested_workflows[name]~ ~Workflow~ itself. A popped-open nested editor uses the
       same gathering logic (~exec::gather_inputs~) via ~EditorState::simulated_inputs~ to preview
       argument values interactively (see below).
     - [X] A call site whose ~workflow_name~ is currently checked out (its editor is pushed on the
       stack -- see below) is simply treated as not-ready and skipped, not as an error; it becomes
       dispatchable again the moment that editor is popped and the definition moved back
       (~nested::close~ marks every such call site dirty).
     - [X] Individual node dispatch (calling a plugin function) still goes through the existing
       global thread pool exactly as today -- only ~Arc<OrcHandle>~ ever crosses a thread
       boundary, same as always.

     *Opening a nested workflow (double-click a NestedCall node):*
     - [X] No cloning. ~DaggerApp~ holds a ~Vec<StackEntry>~ (a stack of ~EditorState~, each
       tagged with the name it was opened under) instead of a single ~EditorState~. Opening a call
       node *moves* the real ~Workflow~ out of the current top level's ~nested_workflows~ map via
       the new ~take_nested_workflow~ accessor into a fresh ~EditorState::from_workflow(taken)~
       pushed onto the stack -- genuine ownership transfer, not a copy.
     - [X] Because the entry is actually removed from the map while checked out, opening it a
       second time from a different NestedCall node referencing the same name is structurally
       impossible -- "already open elsewhere is disallowed" falls out for free, no separate
       tracking-by-name needed.
     - [X] Opens in a separate OS window via ~ctx.show_viewport_immediate~ (matches the Inspect
       and Constant pop-outs already built).
     - [X] The current argument values feeding the NestedCall node's own input pins (in the level
       below) are gathered once, into ~EditorState::simulated_inputs~ (positionally matching
       ~Workflow::workflow_input_position~, a new orc_sdk accessor). The pushed editor's own
       executor resolves a dangling, workflow-input-flagged pin from this side table instead of
       treating it as empty. Nothing is ever written into the graph, so there is nothing to bake in
       and nothing to revert.
     - [X] The pushed ~EditorState~ is fully ordinary -- same rendering, layout, interaction,
       context menu, and execution engine as any other level, full feature parity, including
       opening a NestedCall node inside it (same move, one level deeper).
     - [X] Adding/removing/reordering the nested workflow's own inputs or outputs is out of scope
       for this pass -- the pushed editor only edits internal wiring/nodes, not its own parameter
       list. A new right-click menu entry ("<name> (nested)") creates a ~NestedCall~ node against
       an *existing* nested workflow, sized/labeled from its declared inputs/outputs -- there is
       still no UI to author a nested workflow's own interface from scratch (fixtures come from
       pyorc, e.g. ~workflows/gen_test.py~'s ~nested_demo_workflow~).

     *While a nested editor is open (everything below it is frozen):*
     - [X] Every ~EditorState~ below the top of the stack renders read-only (~render::draw~ inside
       ~ui.disable()~) every frame -- no ~interaction::update~, ~context_menu::update~, ~measure~,
       or ~exec::tick~ -- so it stays visible (not blank) but processes no input and runs nothing.
     - [X] A click-catcher over each frozen level's canvas sends ~ViewportCommand::Focus~ to the
       current top-of-stack's viewport id, so clicking, dragging, or double-clicking anywhere in a
       buried ancestor window (including the root) just brings the active editor forward. The
       root's own OS close button gets the same redirect while frozen.
     - [X] Because everything below the top is fully paused, none of it is running its own
       background executor while a descendant has a nested workflow checked out -- the
       temporarily-missing ~nested_workflows~ entry is never observed by anything that's actually
       ticking.

     *Closing the pop-out:*
     - [X] No cloning means no separate commit step. Closing pops the stack and moves the
       (possibly edited) ~Workflow~ straight back via ~put_nested_workflow~. The edit already
       happened in place; there is nothing to save at this point and no discard-vs-save prompt for
       the pop-out itself -- save stays a single, top-level-only decision.
     - [X] The popped editor's own ~dirty~ flag ORs into the level below on pop, all the way to
       root, so the existing title-bar asterisk and ~file_menu::confirm_discard~/save flow (already
       built for the main window) cover the whole stack without any new save branch.
     - [X] ~mark_dirty~ every ~NestedCall~ node in the level below that references the closed name
       (not just the one originally double-clicked, since the definition is shared by every
       caller), so background execution reruns them against the (possibly changed) definition. The
       "skip the rerun if already settled" optimization mentioned here previously was not built --
       still a reasonable future nice-to-have, not a correctness gap.

     *Window tracking:*
     - [X] ~DaggerApp~ holds the ~Vec<StackEntry>~ stack described above; the last element is the
       active/interactive one, everything before it is frozen per above.
     - [X] Main window's own unsaved-changes close prompt (Discard / Save) -- already implemented
       (~file_menu::confirm_discard~), reused as-is against the root's ~dirty~ flag, which now
       reflects the whole stack.

     *orc_sdk additions (~rust/orc_sdk/src/dag/workflow.rs~):* small, additive, no behavior change
     to any existing method --
     - [X] ~take_nested_workflow(&mut self, name) -> Option<Workflow>~ / ~put_nested_workflow(&mut
       self, name, workflow)~ -- the move-in/move-out pair everything above is built on.
     - [X] ~nested_workflow_names(&self) -> impl Iterator<Item = &str>~ -- for the context menu's
       "call an existing nested workflow" entries.
     - [X] ~workflow_input_position(&self, ih) -> Result<Option<usize>, DagError>~ -- lets the
       executor recognize a dangling pin as "workflow input i" and feed it a simulated/real value
       instead of nothing.

**** Future (out of scope for now)

     - 3D geometry inspect via wgpu PaintCallback
     - Undo/redo (approach TBD -- possibly reversible user actions / diffs, not decided yet)

*** DONE Crate: rust/dagger/
    CLOSED: [2026-09-07 Mon 23:06]
    :LOGBOOK:
    - State "DONE"       from "NEXT"       [2026-09-07 Mon 23:06]
    - State "NEXT"       from "TODO"       [2026-09-07 Mon 23:06]
    - State "TODO"       from              [2026-09-07 Mon 22:28]
    :END:

    New workspace member. Dependencies: eframe 0.31, egui 0.31, orc_sdk.

    #+begin_src
    rust/dagger/
      Cargo.toml
      src/
        main.rs             -- eframe entry, host setup (OrcHost, PluginSet, DeckRegistry, handle counter)
        app.rs              -- DaggerApp: eframe::App, top-level layout, window management
        canvas.rs           -- Pan/zoom canvas, coordinate transforms (screen <-> canvas)
        render.rs           -- Draw node bodies, title bars, pins, labels, cubic Bezier links
        interaction.rs      -- Pin connect/disconnect, select, delete, box-select
        state.rs            -- EditorState: Workflow + properties + computed outputs + layout
        layout.rs           -- Force-directed layout with DAG flow constraint, spatial hash grid
        context_menu.rs     -- Right-click menu with search field, pluggable FunctionFilter trait
        inspect.rs          -- Inspect node display: scrollable text, resizable viewport, pop-out
        exec.rs             -- Background thread execution via mpsc channels
    #+end_src

    <2026-09-07 Mon 23:06> Created a stub crate, not implemented anything yet.

*** DONE Changes to orc_sdk
    CLOSED: [2026-09-07 Mon 22:58]
    :PROPERTIES:
    :CREATED:  2026-09-07 Mon 22:58
    :END:
    :LOGBOOK:
    - State "DONE"       from "NEXT"       [2026-09-07 Mon 22:58]
    - State "NEXT"       from "TODO"       [2026-09-07 Mon 22:28]
    - State "TODO"       from              [2026-09-07 Mon 22:28]
    :END:

    Add public accessors to Workflow (workflow.rs) since fields are pub(crate):
    - [X] ~nodes() -> impl Iterator<Item = NH>~ -- iterate non-deleted nodes
    - [X] ~node_inputs(NH) -> impl Iterator<Item = IH>~ -- delegate to graph
    - [X] ~node_outputs(NH) -> impl Iterator<Item = OH>~ -- delegate to graph
    - [X] ~node_info(NH) -> Result<Ref<NodeInfo>>~ -- borrow node info - I changed my mind about
      this one. It felt very awkward to return a ref. Cannot return a reference because the borrow
      won't live long enough. I think the caller should decide how long the borrow should
      live. E.g. if they want to read many node infos back to back, they can do that with one borrow
      rather than many repeated borrows. So instead, I added ~node_info_prop~ that returns the
      property (cheap to clone).

    - [X] ~node_from_input(IH) -> NH~ -- which node owns this input pin
    - [X] ~node_from_output(OH) -> NH~ -- which node owns this output pin
    - [X] ~links() -> impl Iterator<Item = (OH, IH)>~ -- iterate non-deleted links
    - [X] ~add_inspect(label) -> Result<(NH, IH)>~ -- create inspect node (1 input, 0 outputs)
    - [X] ~delete_node(NH)~ -- disconnect all pins, mark node and its pins as
    - [X] Add ~Hash~ derive to NH, IH, OH, LH in graph.rs.

    <2026-09-07 Mon 22:54> Did all the above. Marking as done, after a review.

** Vague sketch using egui + eframe + wgpu
   :PROPERTIES:
   :CREATED:  2026-08-26 Wed 11:22
   :END:

   Rust app using eframe (windowing + event loop), egui (immediate-mode UI), and wgpu (GPU
   rendering). The node canvas and all widgets are egui. Inspect nodes embed custom wgpu render
   passes for 2D/3D viewports via egui's PaintCallback. Inspect views can be maximized as a
   floating egui::Window panel, or on native, popped out into a real OS window. On WASM the same
   code runs in a browser tab — the only difference is that pop-out becomes a floating panel
   instead of an OS window. The whole stack compiles to both native and WASM without forking the
   codebase.

