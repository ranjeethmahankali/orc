#+title: Orc
#+author: Ranjeeth Mahankali

* TODO Versioning and Backwards Compatibility
  :PROPERTIES:
  :CREATED:  2026-06-06 Sat 19:25
  :END:

  I have some vague inklings we should just break up our blocks into multiple
  DLLs grouped by topic, and just load an older version compiled DLL when the
  user tries to run an older block. Of course, this only works when the datatype
  hasn't changed, and only the behavior has changed.

  Whatever system we design, it should work for both functions and types, and
  gracefully error out when the user tries to run an unsupported combination of
  versions fo things.

  Figuring this out requires a lot of work.

** TODO Get Clarity from nTop Product Team
   :PROPERTIES:
   :CREATED:  2026-06-06 Sat 19:28
   :END:

** TODO Figure out how to version types
   :PROPERTIES:
   :CREATED:  2026-06-06 Sat 19:28
   :END:

* TODO Deferred failures in niche scenarios
  :PROPERTIES:
  :CREATED:  2026-06-07 Sun 07:55
  :END:

  If we want to migrate to a nicer system that does more things at runtime
  instead of compile time, in some very niche scenarios, we MAY have to live
  with not knowing type mismatch errors before running some functions. This is
  only the case with types that use some type of polymorphism.

* TODO Thread Safety in C SDK
  :PROPERTIES:
  :CREATED:  2026-06-07 Sun 15:12
  :END:

  Whenever the plugin code receives an output handle from the host, it has to
  verify that the output handle was allocated in the same plugin. Otherwise it
  is unsafe to write into that memory address. If it came from another plugin,
  then this plugin cannot know anything about the datatype or memory layout. To
  check for this, each plugin likely needs to keep a global list of allocated
  decks, and ensure the output handle matches one of them. Access to this global
  list should be thread safe, since the host can call the plugin functions from
  any thread.

  While this is trivial to do in C++ and Rust, it is not trivial to do in a
  cross platform manner in C. For now, I just have a dummy check that always
  returns true (safe). This is good enough for a prototype, but at some point we
  need to figure out a good cross platform thread API for our C SDK.

  Should the host provide the thread API too? How does Parasolid handle it?

* TODO Objectify Test
  :PROPERTIES:
  :CREATED:  2026-06-07 Sun 15:19
  :END:

  =Objectify= is a Grasshopper plugin that I wrote a long time ago. It is a very
  good litmus test to judge the quality of this new system, once enough details
  are fleshed out. =Objectify= implements structs on top of Grasshopper. The
  user can compose arbitrary data into their own struct, pass it around their
  file, and later access members of that struct. It is a minor convenience, and
  was trivial to implement in GH. Trying to do the same in Kratos turned into a
  dumpster fire. That is what makes this a good litmus test.

  <2026-08-01 Sat 18:42> It seems fairly trivial to do this with built-in types, and with types that
  are internal to the plugin that is trying to implement this. Just make a generic dictionary type
  that stores all data in a tagged union, or a heap allocated OOP thing or whatever. And make a
  block that takes variadic inputs of different types, and names, and outputs a this dictionary. The
  first input could be a list of names, and the remaining inputs could each represent one member of
  the object / dictionary. Probably fine to do the usual list processing on such inputs, but
  requires a bit of thought.

  It seems very unwieldy to try and get this to work with types that are opaque to another
  plugin. That would require some sort of shared strategy between the plugins, and possible between
  the host and the plugins, for type erased storage of objects in a container. That can be done in
  theory with something that resembles a v-table, but not worth it. Very ugly, and likely not useful
  at all. This is possible trivially in C# (Grasshopper), because it is C#.

* TODO orc_map_fn Macro in Rust SDK
  :PROPERTIES:
  :CREATED:  2026-08-02 Sun 11:00
  :END:

  Some functions will just have one input and one output, and they operate on a single item, and
  produce a single item. These functions don't need the overhead of ~Combinations~. They can just
  ~map~ the ~items~ of the deck, and just copy the ~marks~ of the deck. And the Rust compiler can
  optimize that code much better. It will be nice to have another proc_macro specialized for these
  types of functions. Something like ~orc_map_fn~.

* TODO String operation plugins
  :PROPERTIES:
  :CREATED:  2026-08-02 Sun 11:16
  :END:

  I have some vague ideas about implementing string operations and passing strings around across the
  ABI as a deck of u8. A deck of u8 can naturally represent a single character, a string such as a
  line of text, or a list of strings such as a several lines read from a file, or a list of list of
  strings, such as lines of text read from several files.

* TODO tj45qx - C++ SDK
  :PROPERTIES:
  :CREATED:  2026-08-01 Sat 18:58
  :END:

  Given nTop's code is all in C++, this is pretty important. LLMs can easily port the Deck
  implementation etc. But the abstractions to remove friction in implementing blocks will require
  some thought. I went with macros as the first pass in Rust. It is not clear to me what I should go
  with in C++.

* NEXT tj5fbs - Figure out C SDK and write a simple plugin
  :PROPERTIES:
  :CREATED:  2026-08-02 Sun 11:09
  :END:
  :LOGBOOK:
  - State "NEXT"       from "TODO"       [2026-08-02 Sun 11:44]
  :END:

  The code for this is mostly in place. I just need to figure out the project structure, the
  experience of various plugins that want to build off of ~orc_sdk~, and that of plugins that don't
  want to build off of ~orc_sdk~ etc. I also need to figure out how to setup all these projects with
  the ~nob~ build system.

  Container operations plugin is a prime candidate for writing in C, because it doesn't need any
  special libraries. Don't want to add too many blocks at once, but just a few to make sure the
  proxy stuff is working nicely across the ABI.

  <2026-08-02 Sun 11:43> orc_sdk should not implement definitions anything from orc_ffi.h. orc_ffi
  should just contain pure forward declarations. It is up to each plugin how to implement
  those. orc_sdk should provide namespaced equivalents of the things in orc_ffi, and each plugin can
  choose to simply call the orc_sdk functions if they don't want to write their own.

* TODO tiwkbm - Create Toy Demo Apps
  :PROPERTIES:
  :CREATED:  2026-07-28 Tue 16:45
  :END:
  :LOGBOOK:
  - State "TODO"       from "NEXT"       [2026-07-31 Fri 20:46]
  - State "NEXT"       from "TODO"       [2026-07-30 Thu 18:36]
  :END:

  Not that the plugin architecture is fleshed out, I can build a few toy host applications, and toy
  plugins, and demonstrate the flexibility and power of this design to both Product / Customer
  facing side as well as the engineering side.

  <2026-07-30 Thu 18:35> Made quite some progress with the orc_fn macro to make it easier to
  implement simple blocks. The macro is now able to handle templated Rust functions too, but still
  requires some more testing before I feel confident about it.

  <2026-07-31 Fri 18:22> The orc_fn proc macro mostly took shape. Trying to simplify the code a bit,
  and maybe add some tests. The proxy deck functions need more thought, and need to be fleshed
  out. Also the type ids require more thought. Plugins will need the ability to work with arbitrary
  types defined in other plugins. Which means plugins cannot rely on their own type ids. The handle
  cannot just communicate OrcTypeId. It needs to have a unique integer id that the host assigns to
  that type, after loading all the plugins, and the plugins need to know that mapped id for their
  own types after plugin_init.... It will get a bit more complicated than it currently is.

  <2026-07-31 Fri 20:45> Trying to temporarily stop on this task, and finish a few other smaller
  tasks before continuing.

* DONE tj2fe7 - Proxy decks through the ABI
  CLOSED: [2026-08-02 Sun 11:23]
  :PROPERTIES:
  :CREATED:  2026-07-31 Fri 20:44
  :END:
  :LOGBOOK:
  - State "DONE"       from "NEXT"       [2026-08-02 Sun 11:23]
  - State "NEXT"       from "TODO"       [2026-08-01 Sat 18:50]
  :END:

  Currently proxy deck stuff is not fully fleshed out through the ABI. Requires a bit more thought
  and [[*tj2fch - OrcHandle type_id rework][type_id rework]] needs to happen before this. I will just flesh this out first in the Rust SDK,
  and Rust examples. The C SDK has not taken shape yet.

  <2026-08-01 Sat 21:08> Started implementing it. Made some progress. Close to finishing. But hit a
  small snag. I realized that the refactor I just made to the type id system changes some of the
  ownership semantics. It allows many plugins to deal with the same type ids. Which means the host
  cannot clearly determine which plugin is supposed to free a deck. The deck MUST be freed by the
  same plugin that created it. Because only that plugin knows the implementation of the deck
  datastructure it used. Which means each handle needs to contain a pointer to a free function, that
  takes the u64 handle, and returns an error. The dll need not export the free function. This
  changes the ABI design. I need to figure this out before making progress on the proxy stuff

  <2026-08-02 Sun 10:56> Updated the ownership semantics of handles. The handles now carry their own
  free functions. Finished implementing the proxy stuff in the Rust plugin. Not tested it yet. I am
  considering merging this in, and testing it later as I write more plugins.

* DONE tj2fch - OrcHandle type_id rework
  CLOSED: [2026-08-01 Sat 18:36]
  :PROPERTIES:
  :CREATED:  2026-07-31 Fri 20:43
  :END:
  :LOGBOOK:
  - State "DONE"       from "NEXT"       [2026-08-01 Sat 18:36]
  - State "NEXT"       from "TODO"       [2026-08-01 Sat 17:39]
  :END:

  Need something that allows plugins to receive the host side type id, and allows plugins to receive
  handles containing data from other plugins.

  <2026-08-01 Sat 18:35> Needs more testing as I build more plugins. In as good of a shape as it can
  be at this early stage. Preparing to merge.

** DONE ABI / DLL Versioning
   CLOSED: [2026-08-01 Sat 17:40]
   :PROPERTIES:
   :CREATED:  2026-07-31 Fri 18:58
   :END:
   :LOGBOOK:
   - State "DONE"       from "NEXT"       [2026-08-01 Sat 17:40]
   - State "NEXT"       from "TODO"       [2026-07-31 Fri 19:29]
   :END:

   ABI itself should be protected from changes as much as possible. When it changes, all binaries,
   plugins, and DLLs must be recompiled. Some minor changes like adding a new function might not
   require this, but we need a water tight strategy to avoid loading running binaries that target a
   different ABI than the host. That can cause crashes.

   My first idea is to include a version integer in the header. That integer can pack all three
   values of a semantic version number in the bits of a 64 bit integer.

   <2026-07-31 Fri 19:29> Trying to take a crack at this whole prototyping demo apps.

   <2026-08-01 Sat 17:39> Turning this into a subtask, because it will get merged as part of another
   task. Marking it done. The design I have on this branch seems good enough for now.

* CLOSED Rendering Geometry
  CLOSED: [2026-07-28 Tue 09:16]
  :PROPERTIES:
  :CREATED:  2026-06-06 Sat 19:21
  :END:
  :LOGBOOK:
  - State "CLOSED"     from "TODO"       [2026-07-28 Tue 09:16]
  :END:

  The current rendering API in [[file:c/src/orc_ffi.h::rendering_api][the current ffi]] is just a stub. While supporting
  static rendering primitives is simple, supporting implicit rendering requires
  a lot more thought.

  <2026-07-28 Tue 09:14> I changed my mind about this. I realized each host application will have
  very specialized rendering needs, and those needs cannot be met by a single API. So we're better
  off going the Parasolid route. Just expose functions that return data in a form that the host can
  render. E.g. meshes should return triangles, and implicit bodies should return a handle to some
  type of evaluator that can be used fill up voxel grids. In theory, should plug in to a system like
  Aether just fine. I will close this for now.

* DONE Unit System
  CLOSED: [2026-07-28 Tue 09:13]
  :PROPERTIES:
  :CREATED:  2026-06-06 Sat 19:20
  :END:
  :LOGBOOK:
  - State "DONE"       from "TODO"       [2026-07-28 Tue 09:13]
  :END:

  I awkwardly shoved the unit system in the DeckHandle. This needs a lot of
  thought and needs to be done properly.

  <2026-07-28 Tue 09:12> I tweaked this as I went along. It is now in a decent shape. Unless some
  very specific requirements come up, the current state is good enough to replace what we have in
  Kratos.

* DONE tgp7lf - Plugin Architecture
  CLOSED: [2026-07-27 Mon 08:45]
  :PROPERTIES:
  :CREATED:  2026-06-15 Mon 20:19
  :END:
  :LOGBOOK:
  - State "DONE"       from "NEXT"       [2026-07-27 Mon 08:45]
  - State "NEXT"       from "TODO"       [2026-07-04 Sat 11:42]
  - State "TODO"       from "NEXT"       [2026-07-03 Fri 11:08]
  - State "NEXT"       from "TODO"       [2026-06-15 Mon 20:30]
  :END:

  The basic datastructure stuff is coming out nicely, but still, all the C code is one place, and
  the all the Rust code is in one crate. I need to make clean boundary between the host programs,
  the ABI layer, and the plugin binaries. The Rust side of this task is non-trivial and will require
  some thought.

  <2026-07-03 Fri 11:10> Started figuring out the solution for generic containers, while working
  through this task. Will get that done as a separate task and return to this later.

  <2026-07-04 Sat 11:42> I want to use bindgen to generate the ABI layer in all languages from a
  single source of truth.

  <2026-07-23 Thu 23:42> Made decent progress using bindgen to establish a single source of truth
  for the ABI a few weeks ago. Also making slight progress toward a host / plugin split in the Rust
  SDK. The =orc_plugin= macro is the key to make this work. Every Rust plugin will need to call this
  macro exactly once in their crate. The macro expansion will implement ABI compatible C functions
  based on Rust trait implementation.

  The proxy deck stuff (how generic container operations work) is a little behind in Rust compared
  to the C SDK. Left some todos in the code. I need to finish that up before continuing with the
  API.

  <2026-07-25 Sat 16:08> Untested stubs mostly sketched out for dealing with proxy decks in
  Rust. Need to separate out the SDK library crate from concrete plugin crates to be able to test
  it.

  <2026-07-25 Sat 19:52> Started implementing =DeckRegistry=. Made me realize assumptions around
  concurrency require a lot more thought. Host programs will generally take responsibility for
  running things in the correct order, keeping track of dependencies etc. But the plugins also need
  to be able to assert their own safety, locks etc. I am trying to figure out if there is a common
  denominator of behavior + checking that all plugins will use, that I can include in the SDK.

  <2026-07-26 Sun 23:02> Figured out quite a few details. Made a lot of progress today. The registry
  is now super generic, and can be used with any type, not just decks. Got a toy plugin crate to
  build in Rust, and a toy host program to build in Rust. Verified that the crate can load the
  shared library, and run the plugin function properly. The host is also able to load the metadata
  for the plugin function. I think I am close to merging this. Will do a final review, and add some
  tests before merging.

  <2026-07-27 Mon 08:44> This is looking pretty good. Could use some more tests, but not important
  right now. I will let more things take shape before nailing down testing.

* DONE thlu2u - Generic Container Operations
  CLOSED: [2026-07-03 Fri 19:12]
  :PROPERTIES:
  :CREATED:  2026-07-03 Fri 11:08
  :END:
  :LOGBOOK:
  - State "DONE"       from "NEXT"       [2026-07-03 Fri 19:12]
  - State "NEXT"       from "TODO"       [2026-07-03 Fri 11:13]
  :END:

  A plugin needs to be able to implement generic container operations that work
  with datatypes that are not owned by that plugin, across the DLL
  boundary. This is not so trivial. While trying to figure out the plugin
  architecture, I came up with the idea of using an API that is similar to
  LLVM's vector shuffle instruction.

  <2026-07-03 Fri 12:16> Wrote an untested implementation in C to create decks
  from proxy decks.

  <2026-07-03 Fri 19:12> Tested it with LLM generated tests, and fixed
  issues. Ready to merge and mark this done.

* DONE tgah09 - Combinations: Helper functions to stride along decks
  CLOSED: [2026-06-08 Mon 18:26]
  :PROPERTIES:
  :CREATED:  2026-06-07 Sun 08:36
  :END:
  :LOGBOOK:
  - State "DONE"       from "NEXT"       [2026-06-08 Mon 18:26]
  - State "NEXT"       from "TODO"       [2026-06-07 Sun 16:11]
  :END:

  <2026-06-07 Sun 08:36> Write a stubbed out API for combinations. Starting to
  take shape in C.

  <2026-06-07 Sun 16:11> Made some more progress. Have a rough sketch of what
  the API should look like, and the mechanism.

  <2026-06-07 Sun 21:23> Finished writing out the entire logic of combinations
  with zero testing done so far. Made a lot of progress. This also forced me to
  get more clarity on the ownership of decks and handles.

  <2026-06-08 Mon 17:32> Tested with several combinations of inputs. Working
  beautifully.

  <2026-06-14 Sun 18:08> Also wrote and tested a Rust implementation, that
  resembles the C implementation.

** DONE Use matrices for storage of views and writers
   CLOSED: [2026-06-08 Mon 18:26]
   :PROPERTIES:
   :CREATED:  2026-06-07 Sun 20:11
   :END:
   :LOGBOOK:
   - State "DONE"       from "TODO"       [2026-06-08 Mon 18:26]
   :END:

   [[file:./c/src/orc_sdk.c::// TODO - Use matrices for storage of views and writers. view_stacks, and][Change here]]

   =view_stacks=, and =writer_stacks= arrays nested arrays are rectangular, so it
   should be allocated as one buffer with matrix style indexing. This has a lot
   of benefits:
   - This will move all the potential allocation failures all into one place
     inside comb_init, because that is the only place allocations
     happen.
   - Currently I have a set of asserts to make sure the stacks corresponding to
     different inputs and outputs are all of the same depth. All that mess, can
     be simplified by having just one integer to keep track of the depth of all
     stacks. This design will make a bunch of invalid states impossible to
     represent in this datastructure.

  <2026-06-08 Mon> Just did this refactor. Simplified the code, works
  beautifully. The test cases of list processing pass.

** DONE comb_init should take explicit array lengths
   CLOSED: [2026-06-08 Mon 14:07]
   :PROPERTIES:
   :CREATED:  2026-06-08 Mon 13:58
   :END:
   :LOGBOOK:
   - State "DONE"       from "TODO"       [2026-06-08 Mon 14:07]
   :END:

   [[file:c/src/orc_sdk.c::// TODO: Update this function to take input and output counts explicitly instead of][Change here]]

   <2026-06-08 Mon 14:07> This was trivial, just did it, and it worked.
